/**
 * Generated SCXML State Machine: CoRe384HeadSM
 * 
 * This file was automatically generated by scxml-gen.
 * Do not edit manually.
 * 
 * @see https://www.w3.org/TR/scxml/
 */

import { ScxmlStateMachine } from './scxml-runtime.js';

export class CoRe384HeadSM extends ScxmlStateMachine {
    // State IDs
    static S_OPERATIONAL = 'operational';
    static S_NOT_INITIALIZED = 'not_initialized';
    static S_IDLE384 = 'idle384';
    static S_IDLE384_HISTORY = 'idle384_history';
    static S_NO_TIPS384 = 'no_tips384';
    static S_TIPS_ON384 = 'tips_on384';
    static S_TIPS_EMPTY384 = 'tips_empty384';
    static S_TIPS_LOADED384 = 'tips_loaded384';
    static S_MOVING384 = 'moving384';
    static S_WASHING384 = 'washing384';
    static S_ERROR384 = 'error384';

    // Event IDs for optimized dispatch
    // Use these constants with sendById() for maximum performance
    static EVT_MOVE384_DONE = 13;
    static EVT_C0JU = 12;
    static EVT_C0EN = 2;
    static EVT_C0QY = 6;
    static EVT_WASH384_DONE = 14;
    static EVT_C0JI = 1;
    static EVT_C0JG = 11;
    static EVT_C0QK = 5;
    static EVT_C0JD = 9;
    static EVT_C0QJ = 4;
    static EVT_C0JC = 10;
    static EVT_C0JB = 7;
    static EVT_C0JA = 8;
    static EVT_C0EY = 3;
    static EVT_UNKNOWN = 9999;

    // Event name to ID mapping for string-to-int conversion
    static EVENT_MAP = new Map([['move384.done', 13], ['C0JU', 12], ['C0EN', 2], ['C0QY', 6], ['wash384.done', 14], ['C0JI', 1], ['C0JG', 11], ['C0QK', 5], ['C0JD', 9], ['C0QJ', 4], ['C0JC', 10], ['C0JB', 7], ['C0JA', 8], ['C0EY', 3]]);

    constructor() {
        super();
        this._name = 'HamiltonSTAR_CoRe384Head';
        this._traceListener = null;
        // Pre-allocated Sets for dispatch (avoids allocation per event)
        this._dispatchSnapshot = new Set();
        this._dispatchAdded = new Set();

        // Data model initialization
        try {
            this._datamodel['tips_fitted'] = this._evalExpr('false', true);
        } catch (__e) {
            this._datamodel['tips_fitted'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['tip_type'] = this._evalExpr('-1', true);
        } catch (__e) {
            this._datamodel['tip_type'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['volume_01ul'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['volume_01ul'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['pos_x'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['pos_x'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['pos_y'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['pos_y'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['pos_z'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['pos_z'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['y_min'] = this._evalExpr('1054', true);
        } catch (__e) {
            this._datamodel['y_min'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['y_max'] = this._evalExpr('5743', true);
        } catch (__e) {
            this._datamodel['y_max'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['last_error'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['last_error'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['lld_mode'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['lld_mode'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
    }

    _enterInitialConfiguration() {
        if (this._traceListener) this._traceListener.onSessionStart(this._sessionid, 'HamiltonSTAR_CoRe384Head', Date.now());
        this._enter_operational();
        if (!this.activeStates.has('operational')) {
            this._enter_operational();
        }
        this._enter_not_initialized();
        this._checkEventlessTransitions();
    }

    _getStateChildren(stateId) {
        const children = {
            'operational': ['not_initialized', 'idle384', 'moving384', 'washing384', 'error384'],
            'idle384': ['no_tips384', 'tips_on384'],
            'tips_on384': ['tips_empty384', 'tips_loaded384'],
        };
        return children[stateId] || [];
    }

    _getStateParent(stateId) {
        const parents = {
            'not_initialized': 'operational',
            'idle384': 'operational',
            'no_tips384': 'idle384',
            'tips_on384': 'idle384',
            'tips_empty384': 'tips_on384',
            'tips_loaded384': 'tips_on384',
            'moving384': 'operational',
            'washing384': 'operational',
            'error384': 'operational',
        };
        return parents[stateId] || null;
    }

    _getStateOrder(stateId) {
        const order = {
            'operational': 0,
            'not_initialized': 1,
            'idle384': 2,
            'no_tips384': 3,
            'tips_on384': 4,
            'tips_empty384': 5,
            'tips_loaded384': 6,
            'moving384': 7,
            'washing384': 8,
            'error384': 9,
        };
        return order[stateId] !== undefined ? order[stateId] : 9999;
    }

    _getEnabledTransitionForState(stateId, event) {
        if (stateId === 'not_initialized') {
            return this._getEnabledTransition_not_initialized(event);
        } else if (stateId === 'idle384') {
            return this._getEnabledTransition_idle384(event);
        } else if (stateId === 'no_tips384') {
            return this._getEnabledTransition_no_tips384(event);
        } else if (stateId === 'tips_on384') {
            return this._getEnabledTransition_tips_on384(event);
        } else if (stateId === 'tips_empty384') {
            return this._getEnabledTransition_tips_empty384(event);
        } else if (stateId === 'tips_loaded384') {
            return this._getEnabledTransition_tips_loaded384(event);
        } else if (stateId === 'moving384') {
            return this._getEnabledTransition_moving384(event);
        } else if (stateId === 'washing384') {
            return this._getEnabledTransition_washing384(event);
        } else if (stateId === 'error384') {
            return this._getEnabledTransition_error384(event);
        }
        return null;
    }

    _checkEventlessTransitions() {
        let changed = true;
        let iterations = 0;
        const maxIterations = 1000;

        while (changed && iterations < maxIterations) {
            changed = false;
            iterations++;

            // For each atomic state, find first matching eventless transition in lineage
            const enabledEventless = [];
            const addedTransitions = new Set();

            // Helper: get eventless transition for a state
            const getEventless = (stateId) => {
                return null;
            };

            // Find atomic states
            const atomicStates = [];
            for (const stateId of this.activeStates) {
                const children = this._getStateChildren(stateId);
                let hasActiveChild = false;
                for (const childId of children) {
                    if (this.activeStates.has(childId)) { hasActiveChild = true; break; }
                }
                if (!hasActiveChild) atomicStates.push(stateId);
            }

            // For each atomic state, traverse lineage and pick first matching
            atomicStates.sort((a, b) => this._getStateOrder(a) - this._getStateOrder(b));
            for (const atomicState of atomicStates) {
                let currentState = atomicState;
                while (currentState) {
                    const t = getEventless(currentState);
                    if (t) {
                        const key = t.source + ':' + t.priority;
                        if (!addedTransitions.has(key)) {
                            addedTransitions.add(key);
                            enabledEventless.push(t);
                        }
                        break;
                    }
                    currentState = this._getStateParent(currentState);
                }
            }

            // Filter conflicting and execute microstep
            if (enabledEventless.length > 0) {
                enabledEventless.sort((a, b) => a.priority - b.priority);
                const filtered = this._filterConflictingTransitions(enabledEventless);
                if (filtered.length > 0) {
                    changed = true;
                    // Exit in REVERSE document order
                    const sortedByReverse = [...filtered].sort((a, b) => b.priority - a.priority);
                    for (const t of sortedByReverse) t.exit();
                    // Content and enter in document order
                    filtered.sort((a, b) => a.priority - b.priority);
                    for (const t of filtered) t.content();
                    for (const t of filtered) t.enter();
                }
            }
        }
    }

    _enter_operational() {
        this._enterState('operational');
        if (this._traceListener) this._traceListener.onStateEnter('operational', [...this.activeStates], Date.now());
    }

    _exit_operational() {
        if (this.activeStates.has('error384')) {
            this._exit_error384();
        }
        if (this.activeStates.has('washing384')) {
            this._exit_washing384();
        }
        if (this.activeStates.has('moving384')) {
            this._exit_moving384();
        }
        if (this.activeStates.has('idle384')) {
            this._exit_idle384();
        }
        if (this.activeStates.has('not_initialized')) {
            this._exit_not_initialized();
        }
        if (this._traceListener) this._traceListener.onStateExit('operational', [...this.activeStates], Date.now());
        this._exitState('operational');
    }

    _enter_not_initialized() {
        this._enterState('not_initialized');
        if (this._traceListener) this._traceListener.onStateEnter('not_initialized', [...this.activeStates], Date.now());
    }

    _exit_not_initialized() {
        if (this._traceListener) this._traceListener.onStateExit('not_initialized', [...this.activeStates], Date.now());
        this._exitState('not_initialized');
    }

    _getEnabledTransition_not_initialized(event) {
        if (this._matchEvent(event.name, 'C0JI')) {
            return {
                source: 'not_initialized',
                priority: 1,
                exitSet: new Set(['not_initialized']),
                execute: () => {
                    if (this.activeStates.has('not_initialized')) {
                        this._exit_not_initialized();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('not_initialized', ['no_tips384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tips_fitted', expr: 'false'}, Date.now());
                    this._validateAndAssign('tips_fitted', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_01ul', expr: '0'}, Date.now());
                    this._validateAndAssign('volume_01ul', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '0'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_x', expr: '_event.data.xs||0'}, Date.now());
                    this._validateAndAssign('pos_x', this._evalExpr('_event.data.xs||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_y', expr: '_event.data.yk||0'}, Date.now());
                    this._validateAndAssign('pos_y', this._evalExpr('_event.data.yk||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Init 384 Head\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Init 384 Head\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    this._enter_no_tips384();
                }
            };
        }
        return null;
    }

    _enter_idle384() {
        this._enterState('idle384');
        if (this._traceListener) this._traceListener.onStateEnter('idle384', [...this.activeStates], Date.now());
    }

    _exit_idle384() {
        // Record history
        this._recordHistory('idle384_history', Array.from(this.activeStates).filter(s => s !== 'idle384'));
        if (this.activeStates.has('tips_on384')) {
            this._exit_tips_on384();
        }
        if (this.activeStates.has('no_tips384')) {
            this._exit_no_tips384();
        }
        if (this._traceListener) this._traceListener.onStateExit('idle384', [...this.activeStates], Date.now());
        this._exitState('idle384');
    }

    _getEnabledTransition_idle384(event) {
        if (this._matchEvent(event.name, 'C0EN') && this._evalCond('_event.data.yk===undefined||(_event.data.yk>=y_min&&_event.data.yk<=y_max)')) {
            return {
                source: 'idle384',
                priority: 2,
                exitSet: new Set(['idle384', 'no_tips384', 'tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle384', ['moving384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_x', expr: '_event.data.xs||pos_x'}, Date.now());
                    this._validateAndAssign('pos_x', this._evalExpr('_event.data.xs||pos_x', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_y', expr: '_event.data.yk||pos_y'}, Date.now());
                    this._validateAndAssign('pos_y', this._evalExpr('_event.data.yk||pos_y', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_z', expr: '_event.data.je!==undefined?_event.data.je:(_event.data.zf!==undefined?_event.data.zf:pos_z)'}, Date.now());
                    this._validateAndAssign('pos_z', this._evalExpr('_event.data.je!==undefined?_event.data.je:(_event.data.zf!==undefined?_event.data.zf:pos_z)', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Move X=\'+pos_x+\' Y=\'+pos_y+\' Z=\'+pos_z'}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Move X=\'+pos_x+\' Y=\'+pos_y+\' Z=\'+pos_z'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_moving384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0EN') && this._evalCond('_event.data.yk!==undefined&&(_event.data.yk<y_min||_event.data.yk>y_max)')) {
            return {
                source: 'idle384',
                priority: 2,
                exitSet: new Set(['idle384', 'no_tips384', 'tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle384', ['error384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '27'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('27', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'Y out of range (error 27)\''}, Date.now());
                    this._log('ERROR', this._evalExpr('\'Y out of range (error 27)\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_error384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0EY')) {
            return {
                source: 'idle384',
                priority: 2,
                exitSet: new Set(['idle384', 'no_tips384', 'tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle384', ['idle384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_y', expr: '_event.data.yk||pos_y'}, Date.now());
                    this._validateAndAssign('pos_y', this._evalExpr('_event.data.yk||pos_y', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Safety move Y=\'+pos_y'}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Safety move Y=\'+pos_y'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle384();
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    this._enter_no_tips384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0QJ')) {
            return {
                source: 'idle384',
                priority: 2,
                exitSet: new Set(['idle384', 'no_tips384', 'tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle384', ['idle384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Position: X=\'+pos_x+\' Y=\'+pos_y+\' Z=\'+pos_z'}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Position: X=\'+pos_x+\' Y=\'+pos_y+\' Z=\'+pos_z'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle384();
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    this._enter_no_tips384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0QK')) {
            return {
                source: 'idle384',
                priority: 2,
                exitSet: new Set(['idle384', 'no_tips384', 'tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle384', ['idle384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Tips: \'+(tips_fitted?\'fitted\':\'none\')'}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Tips: \'+(tips_fitted?\'fitted\':\'none\')'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle384();
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    this._enter_no_tips384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0QY')) {
            return {
                source: 'idle384',
                priority: 2,
                exitSet: new Set(['idle384', 'no_tips384', 'tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle384', ['idle384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Head type: CO-RE 384\''}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Head type: CO-RE 384\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle384();
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    this._enter_no_tips384();
                }
            };
        }
        return null;
    }

    _enter_idle384_history() {
        const history = this._getHistory('idle384_history');
        if (history.size > 0) {
            // Restore saved history
            for (const stateId of history) {
                const enterFn = this['_enter_' + stateId.replace(/-/g, '_').replace(/\./g, '_')];
                if (enterFn) enterFn.call(this);
            }
        } else {
            // No history - use default transition
            if (!this.activeStates.has('operational')) {
                this._enter_operational();
            }
            if (!this.activeStates.has('idle384')) {
                this._enter_idle384();
            }
            this._enter_no_tips384();
        }
    }

    _enter_no_tips384() {
        this._enterState('no_tips384');
        if (this._traceListener) this._traceListener.onStateEnter('no_tips384', [...this.activeStates], Date.now());
    }

    _exit_no_tips384() {
        if (this._traceListener) this._traceListener.onStateExit('no_tips384', [...this.activeStates], Date.now());
        this._exitState('no_tips384');
    }

    _getEnabledTransition_no_tips384(event) {
        if (this._matchEvent(event.name, 'C0JB')) {
            return {
                source: 'no_tips384',
                priority: 4,
                exitSet: new Set(['no_tips384']),
                execute: () => {
                    if (this.activeStates.has('no_tips384')) {
                        this._exit_no_tips384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('no_tips384', ['tips_empty384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tips_fitted', expr: 'true'}, Date.now());
                    this._validateAndAssign('tips_fitted', this._evalExpr('true', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_type', expr: '_event.data.tt||0'}, Date.now());
                    this._validateAndAssign('tip_type', this._evalExpr('_event.data.tt||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Tip pickup type=\'+(_event.data.tt||0)'}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Tip pickup type=\'+(_event.data.tt||0)'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    if (!this.activeStates.has('tips_on384')) {
                        this._enter_tips_on384();
                    }
                    this._enter_tips_empty384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0JA')) {
            return {
                source: 'no_tips384',
                priority: 4,
                exitSet: new Set(['no_tips384', 'idle384', 'tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('no_tips384', ['error384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '8'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('8', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'384 No tips (error 08)\''}, Date.now());
                    this._log('ERROR', this._evalExpr('\'384 No tips (error 08)\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_error384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0JD')) {
            return {
                source: 'no_tips384',
                priority: 4,
                exitSet: new Set(['no_tips384', 'idle384', 'tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('no_tips384', ['error384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '8'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('8', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'384 No tips (error 08)\''}, Date.now());
                    this._log('ERROR', this._evalExpr('\'384 No tips (error 08)\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_error384();
                }
            };
        }
        return null;
    }

    _enter_tips_on384() {
        this._enterState('tips_on384');
        if (this._traceListener) this._traceListener.onStateEnter('tips_on384', [...this.activeStates], Date.now());
    }

    _exit_tips_on384() {
        if (this.activeStates.has('tips_loaded384')) {
            this._exit_tips_loaded384();
        }
        if (this.activeStates.has('tips_empty384')) {
            this._exit_tips_empty384();
        }
        if (this._traceListener) this._traceListener.onStateExit('tips_on384', [...this.activeStates], Date.now());
        this._exitState('tips_on384');
    }

    _getEnabledTransition_tips_on384(event) {
        if (this._matchEvent(event.name, 'C0JC')) {
            return {
                source: 'tips_on384',
                priority: 5,
                exitSet: new Set(['tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('tips_on384')) {
                        this._exit_tips_on384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tips_on384', ['no_tips384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tips_fitted', expr: 'false'}, Date.now());
                    this._validateAndAssign('tips_fitted', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_01ul', expr: '0'}, Date.now());
                    this._validateAndAssign('volume_01ul', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Tip eject\''}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Tip eject\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    this._enter_no_tips384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0JG')) {
            return {
                source: 'tips_on384',
                priority: 5,
                exitSet: new Set(['tips_on384', 'tips_empty384', 'tips_loaded384', 'idle384', 'no_tips384']),
                execute: () => {
                    if (this.activeStates.has('idle384')) {
                        this._exit_idle384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tips_on384', ['washing384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_01ul', expr: '0'}, Date.now());
                    this._validateAndAssign('volume_01ul', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Wash\''}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Wash\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_washing384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0JU')) {
            return {
                source: 'tips_on384',
                priority: 5,
                exitSet: new Set(['tips_on384', 'tips_empty384', 'tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('tips_on384')) {
                        this._exit_tips_on384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tips_on384', ['tips_empty384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_01ul', expr: '0'}, Date.now());
                    this._validateAndAssign('volume_01ul', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Empty washed tips\''}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Empty washed tips\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    if (!this.activeStates.has('tips_on384')) {
                        this._enter_tips_on384();
                    }
                    this._enter_tips_empty384();
                }
            };
        }
        return null;
    }

    _enter_tips_empty384() {
        this._enterState('tips_empty384');
        if (this._traceListener) this._traceListener.onStateEnter('tips_empty384', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_01ul', expr: '0'}, Date.now());
            this._validateAndAssign('volume_01ul', this._evalExpr('0', true));
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_tips_empty384() {
        if (this._traceListener) this._traceListener.onStateExit('tips_empty384', [...this.activeStates], Date.now());
        this._exitState('tips_empty384');
    }

    _getEnabledTransition_tips_empty384(event) {
        if (this._matchEvent(event.name, 'C0JA')) {
            return {
                source: 'tips_empty384',
                priority: 6,
                exitSet: new Set(['tips_empty384']),
                execute: () => {
                    if (this.activeStates.has('tips_empty384')) {
                        this._exit_tips_empty384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tips_empty384', ['tips_loaded384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_01ul', expr: '_event.data.af||0'}, Date.now());
                    this._validateAndAssign('volume_01ul', this._evalExpr('_event.data.af||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'lld_mode', expr: '_event.data.lm||0'}, Date.now());
                    this._validateAndAssign('lld_mode', this._evalExpr('_event.data.lm||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Asp vol=\'+(_event.data.af||0)+\' lld=\'+(_event.data.lm||0)'}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Asp vol=\'+(_event.data.af||0)+\' lld=\'+(_event.data.lm||0)'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    if (!this.activeStates.has('tips_on384')) {
                        this._enter_tips_on384();
                    }
                    this._enter_tips_loaded384();
                }
            };
        }
        return null;
    }

    _enter_tips_loaded384() {
        this._enterState('tips_loaded384');
        if (this._traceListener) this._traceListener.onStateEnter('tips_loaded384', [...this.activeStates], Date.now());
    }

    _exit_tips_loaded384() {
        if (this._traceListener) this._traceListener.onStateExit('tips_loaded384', [...this.activeStates], Date.now());
        this._exitState('tips_loaded384');
    }

    _getEnabledTransition_tips_loaded384(event) {
        if (this._matchEvent(event.name, 'C0JD') && this._evalCond('_event.data.da==2||_event.data.da==3')) {
            return {
                source: 'tips_loaded384',
                priority: 7,
                exitSet: new Set(['tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('tips_loaded384')) {
                        this._exit_tips_loaded384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tips_loaded384', ['tips_loaded384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_01ul', expr: 'Math.max(0,volume_01ul-(_event.data.df||0))'}, Date.now());
                    this._validateAndAssign('volume_01ul', this._evalExpr('Math.max(0,volume_01ul-(_event.data.df||0))', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Partial disp vol=\'+(_event.data.df||0)'}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Partial disp vol=\'+(_event.data.df||0)'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    if (!this.activeStates.has('tips_on384')) {
                        this._enter_tips_on384();
                    }
                    this._enter_tips_loaded384();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0JD')) {
            return {
                source: 'tips_loaded384',
                priority: 7,
                exitSet: new Set(['tips_loaded384']),
                execute: () => {
                    if (this.activeStates.has('tips_loaded384')) {
                        this._exit_tips_loaded384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tips_loaded384', ['tips_empty384'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_01ul', expr: 'Math.max(0,volume_01ul-(_event.data.df||0))'}, Date.now());
                    this._validateAndAssign('volume_01ul', this._evalExpr('Math.max(0,volume_01ul-(_event.data.df||0))', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'384 Jet disp vol=\'+(_event.data.df||0)'}, Date.now());
                    this._log('FW', this._evalExpr('\'384 Jet disp vol=\'+(_event.data.df||0)'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    if (!this.activeStates.has('tips_on384')) {
                        this._enter_tips_on384();
                    }
                    this._enter_tips_empty384();
                }
            };
        }
        return null;
    }

    _enter_moving384() {
        this._enterState('moving384');
        if (this._traceListener) this._traceListener.onStateEnter('moving384', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('send', {event: 'move384.done', target: '', delay: 'expr=_event.data._delay || \'500ms\''}, Date.now());
            this._send('move384.done', {delay: this._evalExpr('_event.data._delay || \'500ms\''), id: 'move384_timer'});
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_moving384() {
        if (this._traceListener) this._traceListener.onStateExit('moving384', [...this.activeStates], Date.now());
        this._exitState('moving384');
    }

    _getEnabledTransition_moving384(event) {
        if (this._matchEvent(event.name, 'move384.done')) {
            return {
                source: 'moving384',
                priority: 8,
                exitSet: new Set(['moving384', 'operational', 'not_initialized', 'idle384', 'no_tips384', 'tips_on384', 'tips_empty384', 'tips_loaded384', 'washing384', 'error384']),
                execute: () => {
                    if (this.activeStates.has('operational')) {
                        this._exit_operational();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('moving384', ['idle384_history'], event.name, null, Date.now());
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    this._enter_idle384_history();
                }
            };
        }
        return null;
    }

    _enter_washing384() {
        this._enterState('washing384');
        if (this._traceListener) this._traceListener.onStateEnter('washing384', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('send', {event: 'wash384.done', target: '', delay: 'expr=_event.data._delay || \'3000ms\''}, Date.now());
            this._send('wash384.done', {delay: this._evalExpr('_event.data._delay || \'3000ms\''), id: 'wash384_timer'});
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_washing384() {
        if (this._traceListener) this._traceListener.onStateExit('washing384', [...this.activeStates], Date.now());
        this._exitState('washing384');
    }

    _getEnabledTransition_washing384(event) {
        if (this._matchEvent(event.name, 'wash384.done')) {
            return {
                source: 'washing384',
                priority: 9,
                exitSet: new Set(['washing384']),
                execute: () => {
                    if (this.activeStates.has('washing384')) {
                        this._exit_washing384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('washing384', ['tips_empty384'], event.name, null, Date.now());
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle384')) {
                        this._enter_idle384();
                    }
                    if (!this.activeStates.has('tips_on384')) {
                        this._enter_tips_on384();
                    }
                    this._enter_tips_empty384();
                }
            };
        }
        return null;
    }

    _enter_error384() {
        this._enterState('error384');
        if (this._traceListener) this._traceListener.onStateEnter('error384', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'384Head error \'+last_error'}, Date.now());
            this._log('ERROR', this._evalExpr('\'384Head error \'+last_error'));
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_error384() {
        if (this._traceListener) this._traceListener.onStateExit('error384', [...this.activeStates], Date.now());
        this._exitState('error384');
    }

    _getEnabledTransition_error384(event) {
        if (this._matchEvent(event.name, 'C0JI')) {
            return {
                source: 'error384',
                priority: 10,
                exitSet: new Set(['error384']),
                execute: () => {
                    if (this.activeStates.has('error384')) {
                        this._exit_error384();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('error384', ['not_initialized'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '0'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('0', true));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_not_initialized();
                }
            };
        }
        return null;
    }

    /**
     * Get the event ID for an event name.
     * @param {string} eventName - The event name
     * @returns {number} The event ID or EVT_UNKNOWN (9999) if not found
     */
    getEventId(eventName) {
        return this.constructor.EVENT_MAP.get(eventName) ?? this.constructor.EVT_UNKNOWN;
    }

    /**
     * Get the event name for an event ID.
     * @param {number} eventId - The event ID
     * @returns {string|null} The event name or null if unknown
     */
    getEventName(eventId) {
        switch (eventId) {
            case 13: return 'move384.done';
            case 12: return 'C0JU';
            case 2: return 'C0EN';
            case 6: return 'C0QY';
            case 14: return 'wash384.done';
            case 1: return 'C0JI';
            case 11: return 'C0JG';
            case 5: return 'C0QK';
            case 9: return 'C0JD';
            case 4: return 'C0QJ';
            case 10: return 'C0JC';
            case 7: return 'C0JB';
            case 8: return 'C0JA';
            case 3: return 'C0EY';
            default: return null;
        }
    }

    /**
     * Send an event using its integer ID for optimized O(1) dispatch.
     * This bypasses string-to-ID lookup for maximum performance.
     * @param {number} eventId - The event ID (use EVT_* constants)
     * @param {*} [data=null] - Optional event data
     */
    sendById(eventId, data = null) {
        if (this._finished) return;
        const eventName = this.getEventName(eventId);
        if (eventName === null) {
            console.warn('Unknown event ID:', eventId);
            return;
        }
        this.send(eventName, data);
    }

    _matchEvent(eventName, pattern) {
        if (pattern === '*' || pattern === '.*') return true;
        if (pattern === eventName) return true;

        // Prefix matching: 'foo' matches 'foo.bar'
        if (eventName.startsWith(pattern + '.')) return true;

        // Handle space-separated patterns
        const patterns = pattern.split(/\s+/);
        for (const p of patterns) {
            if (p === '*' || p === '.*') return true;
            if (p === eventName) return true;
            if (p.endsWith('.*') && eventName.startsWith(p.slice(0, -2))) return true;
            if (eventName.startsWith(p + '.')) return true;
        }

        return false;
    }

    /**
     * Check if an event ID matches a pattern (internal use).
     * Uses integer comparison for known events, falls back to string for unknown.
     */
    _matchEventById(eventId, eventName, patternId, pattern) {
        // Fast path: wildcard '*' matches everything
        if (pattern === '*' || pattern === '.*') return true;

        // Fast path: exact match by ID (for known events)
        if (eventId !== this.constructor.EVT_UNKNOWN && eventId === patternId) return true;

        // For unknown events or prefix matching, fall back to string comparison
        return this._matchEvent(eventName, pattern);
    }


    static _ALL_EVENTS = new Set(['C0JI', 'C0EN', 'C0EY', 'C0QJ', 'C0QK', 'C0QY', 'C0JB', 'C0JA', 'C0JD', 'C0JC', 'C0JG', 'C0JU', 'move384.done', 'wash384.done']);

    /**
     * Returns all events this state machine can react to.
     * @returns {Set<string>}
     */
    getAllEvents() {
        return new Set(this.constructor._ALL_EVENTS);
    }

    /**
     * Returns events currently enabled based on active states and guard conditions.
     * @returns {Set<string>}
     */
    getEnabledEvents() {
        const enabled = new Set();
        if (this.activeStates.has('not_initialized')) {
            enabled.add('C0JI');
        }
        if (this.activeStates.has('idle384')) {
            try { if (this._evalCond('_event.data.yk===undefined||(_event.data.yk>=y_min&&_event.data.yk<=y_max)')) {
                enabled.add('C0EN');
            } } catch (_ge) { /* guard not evaluable — exclude */ }
            try { if (this._evalCond('_event.data.yk!==undefined&&(_event.data.yk<y_min||_event.data.yk>y_max)')) {
                enabled.add('C0EN');
            } } catch (_ge) { /* guard not evaluable — exclude */ }
            enabled.add('C0EY');
            enabled.add('C0QJ');
            enabled.add('C0QK');
            enabled.add('C0QY');
        }
        if (this.activeStates.has('no_tips384')) {
            enabled.add('C0JB');
            enabled.add('C0JA');
            enabled.add('C0JD');
        }
        if (this.activeStates.has('tips_on384')) {
            enabled.add('C0JC');
            enabled.add('C0JG');
            enabled.add('C0JU');
        }
        if (this.activeStates.has('tips_empty384')) {
            enabled.add('C0JA');
        }
        if (this.activeStates.has('tips_loaded384')) {
            try { if (this._evalCond('_event.data.da==2||_event.data.da==3')) {
                enabled.add('C0JD');
            } } catch (_ge) { /* guard not evaluable — exclude */ }
            enabled.add('C0JD');
        }
        if (this.activeStates.has('moving384')) {
            enabled.add('move384.done');
        }
        if (this.activeStates.has('washing384')) {
            enabled.add('wash384.done');
        }
        if (this.activeStates.has('error384')) {
            enabled.add('C0JI');
        }
        return enabled;
    }

    /**
     * Returns all events a specific state can react to (including ancestor transitions).
     * @param {string} stateId
     * @returns {Set<string>}
     */
    getEventsForState(stateId) {
        switch (stateId) {
            case 'not_initialized': return new Set(['C0JI']);
            case 'idle384': return new Set(['C0EN', 'C0EY', 'C0QJ', 'C0QK', 'C0QY']);
            case 'no_tips384': return new Set(['C0JB', 'C0JA', 'C0JD', 'C0EN', 'C0EY', 'C0QJ', 'C0QK', 'C0QY']);
            case 'tips_on384': return new Set(['C0JC', 'C0JG', 'C0JU', 'C0EN', 'C0EY', 'C0QJ', 'C0QK', 'C0QY']);
            case 'tips_empty384': return new Set(['C0JA', 'C0JC', 'C0JG', 'C0JU', 'C0EN', 'C0EY', 'C0QJ', 'C0QK', 'C0QY']);
            case 'tips_loaded384': return new Set(['C0JD', 'C0JC', 'C0JG', 'C0JU', 'C0EN', 'C0EY', 'C0QJ', 'C0QK', 'C0QY']);
            case 'moving384': return new Set(['move384.done']);
            case 'washing384': return new Set(['wash384.done']);
            case 'error384': return new Set(['C0JI']);
            default: return new Set();
        }
    }

    /**
     * Returns events currently enabled for a specific active state (guard-aware).
     * @param {string} stateId
     * @returns {Set<string>}
     */
    getEnabledEventsForState(stateId) {
        if (!this.activeStates.has(stateId)) return new Set();
        const enabled = new Set();
        switch (stateId) {
            case 'not_initialized': {
                enabled.add('C0JI');
                break;
            }
            case 'idle384': {
                try { if (this._evalCond('_event.data.yk===undefined||(_event.data.yk>=y_min&&_event.data.yk<=y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                try { if (this._evalCond('_event.data.yk!==undefined&&(_event.data.yk<y_min||_event.data.yk>y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                enabled.add('C0EY');
                enabled.add('C0QJ');
                enabled.add('C0QK');
                enabled.add('C0QY');
                break;
            }
            case 'no_tips384': {
                enabled.add('C0JB');
                enabled.add('C0JA');
                enabled.add('C0JD');
                try { if (this._evalCond('_event.data.yk===undefined||(_event.data.yk>=y_min&&_event.data.yk<=y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                try { if (this._evalCond('_event.data.yk!==undefined&&(_event.data.yk<y_min||_event.data.yk>y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                enabled.add('C0EY');
                enabled.add('C0QJ');
                enabled.add('C0QK');
                enabled.add('C0QY');
                break;
            }
            case 'tips_on384': {
                enabled.add('C0JC');
                enabled.add('C0JG');
                enabled.add('C0JU');
                try { if (this._evalCond('_event.data.yk===undefined||(_event.data.yk>=y_min&&_event.data.yk<=y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                try { if (this._evalCond('_event.data.yk!==undefined&&(_event.data.yk<y_min||_event.data.yk>y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                enabled.add('C0EY');
                enabled.add('C0QJ');
                enabled.add('C0QK');
                enabled.add('C0QY');
                break;
            }
            case 'tips_empty384': {
                enabled.add('C0JA');
                enabled.add('C0JC');
                enabled.add('C0JG');
                enabled.add('C0JU');
                try { if (this._evalCond('_event.data.yk===undefined||(_event.data.yk>=y_min&&_event.data.yk<=y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                try { if (this._evalCond('_event.data.yk!==undefined&&(_event.data.yk<y_min||_event.data.yk>y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                enabled.add('C0EY');
                enabled.add('C0QJ');
                enabled.add('C0QK');
                enabled.add('C0QY');
                break;
            }
            case 'tips_loaded384': {
                try { if (this._evalCond('_event.data.da==2||_event.data.da==3')) {
                    enabled.add('C0JD');
                } } catch (_ge) { /* guard not evaluable */ }
                enabled.add('C0JD');
                enabled.add('C0JC');
                enabled.add('C0JG');
                enabled.add('C0JU');
                try { if (this._evalCond('_event.data.yk===undefined||(_event.data.yk>=y_min&&_event.data.yk<=y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                try { if (this._evalCond('_event.data.yk!==undefined&&(_event.data.yk<y_min||_event.data.yk>y_max)')) {
                    enabled.add('C0EN');
                } } catch (_ge) { /* guard not evaluable */ }
                enabled.add('C0EY');
                enabled.add('C0QJ');
                enabled.add('C0QK');
                enabled.add('C0QY');
                break;
            }
            case 'moving384': {
                enabled.add('move384.done');
                break;
            }
            case 'washing384': {
                enabled.add('wash384.done');
                break;
            }
            case 'error384': {
                enabled.add('C0JI');
                break;
            }
            default: break;
        }
        return enabled;
    }


    /**
     * Add a trace listener for debugging/monitoring.
     * @param {Object} listener - Listener object with trace callback methods:
     *   - onSessionStart(sessionId, machineName, timestamp)
     *   - onSessionEnd(sessionId, finalStates, timestamp)
     *   - onStateEnter(stateId, activeStates, timestamp)
     *   - onStateExit(stateId, activeStates, timestamp)
     *   - onTransitionExecute(sourceId, targetIds, event, cond, timestamp)
     *   - onEventReceived(eventName, eventType, timestamp)
     *   - onEventProcessed(eventName, timestamp)
     *   - onActionExecute(actionType, details, timestamp)
     */
    addTraceListener(listener) {
        this._traceListener = listener;
    }

}

// CommonJS export for Node.js compatibility
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { CoRe384HeadSM };
}
