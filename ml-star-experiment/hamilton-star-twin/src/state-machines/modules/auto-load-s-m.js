/**
 * Generated SCXML State Machine: AutoLoadSM
 * 
 * This file was automatically generated by scxml-gen.
 * Do not edit manually.
 * 
 * @see https://www.w3.org/TR/scxml/
 */

import { ScxmlStateMachine } from './scxml-runtime.js';

export class AutoLoadSM extends ScxmlStateMachine {
    // State IDs
    static S_OPERATIONAL = 'operational';
    static S_NOT_INITIALIZED = 'not_initialized';
    static S_IDLE_AL = 'idle_al';
    static S_IDENTIFYING = 'identifying';
    static S_LOADING = 'loading';
    static S_UNLOADING = 'unloading';
    static S_MONITORING = 'monitoring';
    static S_ERROR_AL = 'error_al';

    // Event IDs for optimized dispatch
    // Use these constants with sendById() for maximum performance
    static EVT_C0CU = 10;
    static EVT_C0CT = 6;
    static EVT_C0RC = 16;
    static EVT_C0CS = 7;
    static EVT_C0QA = 17;
    static EVT_C0CR = 4;
    static EVT_C0DR = 15;
    static EVT_C0CQ = 18;
    static EVT_C0CP = 8;
    static EVT_C0IV = 11;
    static EVT_C0CN = 13;
    static EVT_C0CL = 3;
    static EVT_C0CI = 2;
    static EVT_LOAD_ERROR = 22;
    static EVT_C0II = 1;
    static EVT_IDENTIFY_DONE = 20;
    static EVT_C0CB = 9;
    static EVT_LOAD_DONE = 21;
    static EVT_C0CA = 12;
    static EVT_C0DB = 14;
    static EVT_C0VL = 19;
    static EVT_C0CW = 5;
    static EVT_MONITOR_DONE = 24;
    static EVT_UNLOAD_DONE = 23;
    static EVT_UNKNOWN = 9999;

    // Event name to ID mapping for string-to-int conversion
    static EVENT_MAP = new Map([['C0CU', 10], ['C0CT', 6], ['C0RC', 16], ['C0CS', 7], ['C0QA', 17], ['C0CR', 4], ['C0DR', 15], ['C0CQ', 18], ['C0CP', 8], ['C0IV', 11], ['C0CN', 13], ['C0CL', 3], ['C0CI', 2], ['load.error', 22], ['C0II', 1], ['identify.done', 20], ['C0CB', 9], ['load.done', 21], ['C0CA', 12], ['C0DB', 14], ['C0VL', 19], ['C0CW', 5], ['monitor.done', 24], ['unload.done', 23]]);

    constructor() {
        super();
        this._name = 'HamiltonSTAR_AutoLoad';
        this._traceListener = null;
        // Pre-allocated Sets for dispatch (avoids allocation per event)
        this._dispatchSnapshot = new Set();
        this._dispatchAdded = new Set();

        // Data model initialization
        try {
            this._datamodel['carrier_type'] = this._evalExpr('\'none\'', true);
        } catch (__e) {
            this._datamodel['carrier_type'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['carrier_barcode'] = this._evalExpr('\'\'', true);
        } catch (__e) {
            this._datamodel['carrier_barcode'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['carriers_on_deck'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['carriers_on_deck'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['max_carriers'] = this._evalExpr('54', true);
        } catch (__e) {
            this._datamodel['max_carriers'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['tray_occupied'] = this._evalExpr('false', true);
        } catch (__e) {
            this._datamodel['tray_occupied'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['last_error'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['last_error'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['pos_track'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['pos_track'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['target_track'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['target_track'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
    }

    _enterInitialConfiguration() {
        if (this._traceListener) this._traceListener.onSessionStart(this._sessionid, 'HamiltonSTAR_AutoLoad', Date.now());
        this._enter_operational();
        if (!this.activeStates.has('operational')) {
            this._enter_operational();
        }
        this._enter_not_initialized();
        this._checkEventlessTransitions();
    }

    _getStateChildren(stateId) {
        const children = {
            'operational': ['not_initialized', 'idle_al', 'identifying', 'loading', 'unloading', 'monitoring', 'error_al'],
        };
        return children[stateId] || [];
    }

    _getStateParent(stateId) {
        const parents = {
            'not_initialized': 'operational',
            'idle_al': 'operational',
            'identifying': 'operational',
            'loading': 'operational',
            'unloading': 'operational',
            'monitoring': 'operational',
            'error_al': 'operational',
        };
        return parents[stateId] || null;
    }

    _getStateOrder(stateId) {
        const order = {
            'operational': 0,
            'not_initialized': 1,
            'idle_al': 2,
            'identifying': 3,
            'loading': 4,
            'unloading': 5,
            'monitoring': 6,
            'error_al': 7,
        };
        return order[stateId] !== undefined ? order[stateId] : 9999;
    }

    _getEnabledTransitionForState(stateId, event) {
        if (stateId === 'not_initialized') {
            return this._getEnabledTransition_not_initialized(event);
        } else if (stateId === 'idle_al') {
            return this._getEnabledTransition_idle_al(event);
        } else if (stateId === 'identifying') {
            return this._getEnabledTransition_identifying(event);
        } else if (stateId === 'loading') {
            return this._getEnabledTransition_loading(event);
        } else if (stateId === 'unloading') {
            return this._getEnabledTransition_unloading(event);
        } else if (stateId === 'monitoring') {
            return this._getEnabledTransition_monitoring(event);
        } else if (stateId === 'error_al') {
            return this._getEnabledTransition_error_al(event);
        }
        return null;
    }

    _checkEventlessTransitions() {
        let changed = true;
        let iterations = 0;
        const maxIterations = 1000;

        while (changed && iterations < maxIterations) {
            changed = false;
            iterations++;

            // For each atomic state, find first matching eventless transition in lineage
            const enabledEventless = [];
            const addedTransitions = new Set();

            // Helper: get eventless transition for a state
            const getEventless = (stateId) => {
                return null;
            };

            // Find atomic states
            const atomicStates = [];
            for (const stateId of this.activeStates) {
                const children = this._getStateChildren(stateId);
                let hasActiveChild = false;
                for (const childId of children) {
                    if (this.activeStates.has(childId)) { hasActiveChild = true; break; }
                }
                if (!hasActiveChild) atomicStates.push(stateId);
            }

            // For each atomic state, traverse lineage and pick first matching
            atomicStates.sort((a, b) => this._getStateOrder(a) - this._getStateOrder(b));
            for (const atomicState of atomicStates) {
                let currentState = atomicState;
                while (currentState) {
                    const t = getEventless(currentState);
                    if (t) {
                        const key = t.source + ':' + t.priority;
                        if (!addedTransitions.has(key)) {
                            addedTransitions.add(key);
                            enabledEventless.push(t);
                        }
                        break;
                    }
                    currentState = this._getStateParent(currentState);
                }
            }

            // Filter conflicting and execute microstep
            if (enabledEventless.length > 0) {
                enabledEventless.sort((a, b) => a.priority - b.priority);
                const filtered = this._filterConflictingTransitions(enabledEventless);
                if (filtered.length > 0) {
                    changed = true;
                    // Exit in REVERSE document order
                    const sortedByReverse = [...filtered].sort((a, b) => b.priority - a.priority);
                    for (const t of sortedByReverse) t.exit();
                    // Content and enter in document order
                    filtered.sort((a, b) => a.priority - b.priority);
                    for (const t of filtered) t.content();
                    for (const t of filtered) t.enter();
                }
            }
        }
    }

    _enter_operational() {
        this._enterState('operational');
        if (this._traceListener) this._traceListener.onStateEnter('operational', [...this.activeStates], Date.now());
    }

    _exit_operational() {
        if (this.activeStates.has('error_al')) {
            this._exit_error_al();
        }
        if (this.activeStates.has('monitoring')) {
            this._exit_monitoring();
        }
        if (this.activeStates.has('unloading')) {
            this._exit_unloading();
        }
        if (this.activeStates.has('loading')) {
            this._exit_loading();
        }
        if (this.activeStates.has('identifying')) {
            this._exit_identifying();
        }
        if (this.activeStates.has('idle_al')) {
            this._exit_idle_al();
        }
        if (this.activeStates.has('not_initialized')) {
            this._exit_not_initialized();
        }
        if (this._traceListener) this._traceListener.onStateExit('operational', [...this.activeStates], Date.now());
        this._exitState('operational');
    }

    _enter_not_initialized() {
        this._enterState('not_initialized');
        if (this._traceListener) this._traceListener.onStateEnter('not_initialized', [...this.activeStates], Date.now());
    }

    _exit_not_initialized() {
        if (this._traceListener) this._traceListener.onStateExit('not_initialized', [...this.activeStates], Date.now());
        this._exitState('not_initialized');
    }

    _getEnabledTransition_not_initialized(event) {
        if (this._matchEvent(event.name, 'C0II')) {
            return {
                source: 'not_initialized',
                priority: 1,
                exitSet: new Set(['not_initialized']),
                execute: () => {
                    if (this.activeStates.has('not_initialized')) {
                        this._exit_not_initialized();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('not_initialized', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'carriers_on_deck', expr: '0'}, Date.now());
                    this._validateAndAssign('carriers_on_deck', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '0'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_track', expr: '0'}, Date.now());
                    this._validateAndAssign('pos_track', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_track', expr: '0'}, Date.now());
                    this._validateAndAssign('target_track', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Init AutoLoad\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Init AutoLoad\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        return null;
    }

    _enter_idle_al() {
        this._enterState('idle_al');
        if (this._traceListener) this._traceListener.onStateEnter('idle_al', [...this.activeStates], Date.now());
    }

    _exit_idle_al() {
        if (this._traceListener) this._traceListener.onStateExit('idle_al', [...this.activeStates], Date.now());
        this._exitState('idle_al');
    }

    _getEnabledTransition_idle_al(event) {
        if (this._matchEvent(event.name, 'C0II')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CI')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['identifying'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Identify carrier\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Identify carrier\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_identifying();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CL')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['loading'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_track', expr: '_event.data.pq!==undefined?_event.data.pq:1'}, Date.now());
                    this._validateAndAssign('target_track', this._evalExpr('_event.data.pq!==undefined?_event.data.pq:1', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Load carrier to track \'+target_track'}, Date.now());
                    this._log('FW', this._evalExpr('\'Load carrier to track \'+target_track'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_loading();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CR') && this._evalCond('carriers_on_deck > 0')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['unloading'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_track', expr: '0'}, Date.now());
                    this._validateAndAssign('target_track', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Unload carrier from track \'+pos_track'}, Date.now());
                    this._log('FW', this._evalExpr('\'Unload carrier from track \'+pos_track'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_unloading();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CR') && this._evalCond('carriers_on_deck == 0')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['error_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '9'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('9', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'No carrier on deck (error 09)\''}, Date.now());
                    this._log('ERROR', this._evalExpr('\'No carrier on deck (error 09)\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_error_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CW') && this._evalCond('carriers_on_deck > 0')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['unloading'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Unload finally\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Unload finally\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_unloading();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CT')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Check presence\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Check presence\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CS')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Check tray\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Check tray\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CP')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Set LEDs\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Set LEDs\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CB')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Set barcode types\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Set barcode types\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CU')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['monitoring'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Start monitoring\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Start monitoring\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_monitoring();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0IV')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'AutoLoad Z-safe\''}, Date.now());
                    this._log('FW', this._evalExpr('\'AutoLoad Z-safe\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CA')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Push carrier to tray\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Push carrier to tray\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CN')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Move to ID position\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Move to ID position\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0DB')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Set free-definable carrier reading\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Set free-definable carrier reading\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0DR')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Reset free-definable settings\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Reset free-definable settings\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0RC')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Query carrier on deck: \'+carriers_on_deck'}, Date.now());
                    this._log('FW', this._evalExpr('\'Query carrier on deck: \'+carriers_on_deck'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0QA')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Request AutoLoad slot position\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Request AutoLoad slot position\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0CQ')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Request AutoLoad module type\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Request AutoLoad module type\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0VL')) {
            return {
                source: 'idle_al',
                priority: 2,
                exitSet: new Set(['idle_al']),
                execute: () => {
                    if (this.activeStates.has('idle_al')) {
                        this._exit_idle_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_al', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Request code data length\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Request code data length\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        return null;
    }

    _enter_identifying() {
        this._enterState('identifying');
        if (this._traceListener) this._traceListener.onStateEnter('identifying', [...this.activeStates], Date.now());
    }

    _exit_identifying() {
        if (this._traceListener) this._traceListener.onStateExit('identifying', [...this.activeStates], Date.now());
        this._exitState('identifying');
    }

    _getEnabledTransition_identifying(event) {
        if (this._matchEvent(event.name, 'identify.done')) {
            return {
                source: 'identifying',
                priority: 3,
                exitSet: new Set(['identifying']),
                execute: () => {
                    if (this.activeStates.has('identifying')) {
                        this._exit_identifying();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('identifying', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'carrier_type', expr: '_event.data.type||\'unknown\''}, Date.now());
                    this._validateAndAssign('carrier_type', this._evalExpr('_event.data.type||\'unknown\'', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'carrier_barcode', expr: '_event.data.barcode||\'\''}, Date.now());
                    this._validateAndAssign('carrier_barcode', this._evalExpr('_event.data.barcode||\'\'', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Identified: \'+carrier_type+\' bc=\'+carrier_barcode'}, Date.now());
                    this._log('FW', this._evalExpr('\'Identified: \'+carrier_type+\' bc=\'+carrier_barcode'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        return null;
    }

    _enter_loading() {
        this._enterState('loading');
        if (this._traceListener) this._traceListener.onStateEnter('loading', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('send', {event: 'load.done', target: '', delay: 'expr=_event.data._delay || \'4500ms\''}, Date.now());
            this._send('load.done', {delay: this._evalExpr('_event.data._delay || \'4500ms\''), id: 'al_load_timer'});
            if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Loading carrier to track \'+target_track+\' (delay=\' + (_event.data._delay || \'4500ms\') + \')\''}, Date.now());
            this._log('FW', this._evalExpr('\'Loading carrier to track \'+target_track+\' (delay=\' + (_event.data._delay || \'4500ms\') + \')\''));
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_loading() {
        if (this._traceListener) this._traceListener.onStateExit('loading', [...this.activeStates], Date.now());
        this._exitState('loading');
    }

    _getEnabledTransition_loading(event) {
        if (this._matchEvent(event.name, 'load.done')) {
            return {
                source: 'loading',
                priority: 4,
                exitSet: new Set(['loading']),
                execute: () => {
                    if (this.activeStates.has('loading')) {
                        this._exit_loading();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('loading', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'carriers_on_deck', expr: 'carriers_on_deck+1'}, Date.now());
                    this._validateAndAssign('carriers_on_deck', this._evalExpr('carriers_on_deck+1', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_track', expr: 'target_track'}, Date.now());
                    this._validateAndAssign('pos_track', this._evalExpr('target_track', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Carrier loaded, total=\'+carriers_on_deck'}, Date.now());
                    this._log('FW', this._evalExpr('\'Carrier loaded, total=\'+carriers_on_deck'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        if (this._matchEvent(event.name, 'load.error')) {
            return {
                source: 'loading',
                priority: 4,
                exitSet: new Set(['loading']),
                execute: () => {
                    if (this.activeStates.has('loading')) {
                        this._exit_loading();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('loading', ['error_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '9'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('9', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'Load failed (error 09)\''}, Date.now());
                    this._log('ERROR', this._evalExpr('\'Load failed (error 09)\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_error_al();
                }
            };
        }
        return null;
    }

    _enter_unloading() {
        this._enterState('unloading');
        if (this._traceListener) this._traceListener.onStateEnter('unloading', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('send', {event: 'unload.done', target: '', delay: 'expr=_event.data._delay || \'5500ms\''}, Date.now());
            this._send('unload.done', {delay: this._evalExpr('_event.data._delay || \'5500ms\''), id: 'al_unload_timer'});
            if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Unloading carrier from track \'+pos_track+\' (delay=\' + (_event.data._delay || \'5500ms\') + \')\''}, Date.now());
            this._log('FW', this._evalExpr('\'Unloading carrier from track \'+pos_track+\' (delay=\' + (_event.data._delay || \'5500ms\') + \')\''));
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_unloading() {
        if (this._traceListener) this._traceListener.onStateExit('unloading', [...this.activeStates], Date.now());
        this._exitState('unloading');
    }

    _getEnabledTransition_unloading(event) {
        if (this._matchEvent(event.name, 'unload.done')) {
            return {
                source: 'unloading',
                priority: 5,
                exitSet: new Set(['unloading']),
                execute: () => {
                    if (this.activeStates.has('unloading')) {
                        this._exit_unloading();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('unloading', ['idle_al'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'carriers_on_deck', expr: 'Math.max(0,carriers_on_deck-1)'}, Date.now());
                    this._validateAndAssign('carriers_on_deck', this._evalExpr('Math.max(0,carriers_on_deck-1)', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_track', expr: '0'}, Date.now());
                    this._validateAndAssign('pos_track', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Carrier unloaded, total=\'+carriers_on_deck'}, Date.now());
                    this._log('FW', this._evalExpr('\'Carrier unloaded, total=\'+carriers_on_deck'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        return null;
    }

    _enter_monitoring() {
        this._enterState('monitoring');
        if (this._traceListener) this._traceListener.onStateEnter('monitoring', [...this.activeStates], Date.now());
    }

    _exit_monitoring() {
        if (this._traceListener) this._traceListener.onStateExit('monitoring', [...this.activeStates], Date.now());
        this._exitState('monitoring');
    }

    _getEnabledTransition_monitoring(event) {
        if (this._matchEvent(event.name, 'monitor.done')) {
            return {
                source: 'monitoring',
                priority: 6,
                exitSet: new Set(['monitoring']),
                execute: () => {
                    if (this.activeStates.has('monitoring')) {
                        this._exit_monitoring();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('monitoring', ['idle_al'], event.name, null, Date.now());
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_al();
                }
            };
        }
        return null;
    }

    _enter_error_al() {
        this._enterState('error_al');
        if (this._traceListener) this._traceListener.onStateEnter('error_al', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'AutoLoad error \'+last_error'}, Date.now());
            this._log('ERROR', this._evalExpr('\'AutoLoad error \'+last_error'));
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_error_al() {
        if (this._traceListener) this._traceListener.onStateExit('error_al', [...this.activeStates], Date.now());
        this._exitState('error_al');
    }

    _getEnabledTransition_error_al(event) {
        if (this._matchEvent(event.name, 'C0II')) {
            return {
                source: 'error_al',
                priority: 7,
                exitSet: new Set(['error_al']),
                execute: () => {
                    if (this.activeStates.has('error_al')) {
                        this._exit_error_al();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('error_al', ['not_initialized'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '0'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('0', true));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_not_initialized();
                }
            };
        }
        return null;
    }

    /**
     * Get the event ID for an event name.
     * @param {string} eventName - The event name
     * @returns {number} The event ID or EVT_UNKNOWN (9999) if not found
     */
    getEventId(eventName) {
        return this.constructor.EVENT_MAP.get(eventName) ?? this.constructor.EVT_UNKNOWN;
    }

    /**
     * Get the event name for an event ID.
     * @param {number} eventId - The event ID
     * @returns {string|null} The event name or null if unknown
     */
    getEventName(eventId) {
        switch (eventId) {
            case 10: return 'C0CU';
            case 6: return 'C0CT';
            case 16: return 'C0RC';
            case 7: return 'C0CS';
            case 17: return 'C0QA';
            case 4: return 'C0CR';
            case 15: return 'C0DR';
            case 18: return 'C0CQ';
            case 8: return 'C0CP';
            case 11: return 'C0IV';
            case 13: return 'C0CN';
            case 3: return 'C0CL';
            case 2: return 'C0CI';
            case 22: return 'load.error';
            case 1: return 'C0II';
            case 20: return 'identify.done';
            case 9: return 'C0CB';
            case 21: return 'load.done';
            case 12: return 'C0CA';
            case 14: return 'C0DB';
            case 19: return 'C0VL';
            case 5: return 'C0CW';
            case 24: return 'monitor.done';
            case 23: return 'unload.done';
            default: return null;
        }
    }

    /**
     * Send an event using its integer ID for optimized O(1) dispatch.
     * This bypasses string-to-ID lookup for maximum performance.
     * @param {number} eventId - The event ID (use EVT_* constants)
     * @param {*} [data=null] - Optional event data
     */
    sendById(eventId, data = null) {
        if (this._finished) return;
        const eventName = this.getEventName(eventId);
        if (eventName === null) {
            console.warn('Unknown event ID:', eventId);
            return;
        }
        this.send(eventName, data);
    }

    _matchEvent(eventName, pattern) {
        if (pattern === '*' || pattern === '.*') return true;
        if (pattern === eventName) return true;

        // Prefix matching: 'foo' matches 'foo.bar'
        if (eventName.startsWith(pattern + '.')) return true;

        // Handle space-separated patterns
        const patterns = pattern.split(/\s+/);
        for (const p of patterns) {
            if (p === '*' || p === '.*') return true;
            if (p === eventName) return true;
            if (p.endsWith('.*') && eventName.startsWith(p.slice(0, -2))) return true;
            if (eventName.startsWith(p + '.')) return true;
        }

        return false;
    }

    /**
     * Check if an event ID matches a pattern (internal use).
     * Uses integer comparison for known events, falls back to string for unknown.
     */
    _matchEventById(eventId, eventName, patternId, pattern) {
        // Fast path: wildcard '*' matches everything
        if (pattern === '*' || pattern === '.*') return true;

        // Fast path: exact match by ID (for known events)
        if (eventId !== this.constructor.EVT_UNKNOWN && eventId === patternId) return true;

        // For unknown events or prefix matching, fall back to string comparison
        return this._matchEvent(eventName, pattern);
    }


    static _ALL_EVENTS = new Set(['C0II', 'C0CI', 'C0CL', 'C0CR', 'C0CW', 'C0CT', 'C0CS', 'C0CP', 'C0CB', 'C0CU', 'C0IV', 'C0CA', 'C0CN', 'C0DB', 'C0DR', 'C0RC', 'C0QA', 'C0CQ', 'C0VL', 'identify.done', 'load.done', 'load.error', 'unload.done', 'monitor.done']);

    /**
     * Returns all events this state machine can react to.
     * @returns {Set<string>}
     */
    getAllEvents() {
        return new Set(this.constructor._ALL_EVENTS);
    }

    /**
     * Returns events currently enabled based on active states and guard conditions.
     * @returns {Set<string>}
     */
    getEnabledEvents() {
        const enabled = new Set();
        if (this.activeStates.has('not_initialized')) {
            enabled.add('C0II');
        }
        if (this.activeStates.has('idle_al')) {
            enabled.add('C0II');
            enabled.add('C0CI');
            enabled.add('C0CL');
            try { if (this._evalCond('carriers_on_deck > 0')) {
                enabled.add('C0CR');
            } } catch (_ge) { /* guard not evaluable — exclude */ }
            try { if (this._evalCond('carriers_on_deck == 0')) {
                enabled.add('C0CR');
            } } catch (_ge) { /* guard not evaluable — exclude */ }
            try { if (this._evalCond('carriers_on_deck > 0')) {
                enabled.add('C0CW');
            } } catch (_ge) { /* guard not evaluable — exclude */ }
            enabled.add('C0CT');
            enabled.add('C0CS');
            enabled.add('C0CP');
            enabled.add('C0CB');
            enabled.add('C0CU');
            enabled.add('C0IV');
            enabled.add('C0CA');
            enabled.add('C0CN');
            enabled.add('C0DB');
            enabled.add('C0DR');
            enabled.add('C0RC');
            enabled.add('C0QA');
            enabled.add('C0CQ');
            enabled.add('C0VL');
        }
        if (this.activeStates.has('identifying')) {
            enabled.add('identify.done');
        }
        if (this.activeStates.has('loading')) {
            enabled.add('load.done');
            enabled.add('load.error');
        }
        if (this.activeStates.has('unloading')) {
            enabled.add('unload.done');
        }
        if (this.activeStates.has('monitoring')) {
            enabled.add('monitor.done');
        }
        if (this.activeStates.has('error_al')) {
            enabled.add('C0II');
        }
        return enabled;
    }

    /**
     * Returns all events a specific state can react to (including ancestor transitions).
     * @param {string} stateId
     * @returns {Set<string>}
     */
    getEventsForState(stateId) {
        switch (stateId) {
            case 'not_initialized': return new Set(['C0II']);
            case 'idle_al': return new Set(['C0II', 'C0CI', 'C0CL', 'C0CR', 'C0CW', 'C0CT', 'C0CS', 'C0CP', 'C0CB', 'C0CU', 'C0IV', 'C0CA', 'C0CN', 'C0DB', 'C0DR', 'C0RC', 'C0QA', 'C0CQ', 'C0VL']);
            case 'identifying': return new Set(['identify.done']);
            case 'loading': return new Set(['load.done', 'load.error']);
            case 'unloading': return new Set(['unload.done']);
            case 'monitoring': return new Set(['monitor.done']);
            case 'error_al': return new Set(['C0II']);
            default: return new Set();
        }
    }

    /**
     * Returns events currently enabled for a specific active state (guard-aware).
     * @param {string} stateId
     * @returns {Set<string>}
     */
    getEnabledEventsForState(stateId) {
        if (!this.activeStates.has(stateId)) return new Set();
        const enabled = new Set();
        switch (stateId) {
            case 'not_initialized': {
                enabled.add('C0II');
                break;
            }
            case 'idle_al': {
                enabled.add('C0II');
                enabled.add('C0CI');
                enabled.add('C0CL');
                try { if (this._evalCond('carriers_on_deck > 0')) {
                    enabled.add('C0CR');
                } } catch (_ge) { /* guard not evaluable */ }
                try { if (this._evalCond('carriers_on_deck == 0')) {
                    enabled.add('C0CR');
                } } catch (_ge) { /* guard not evaluable */ }
                try { if (this._evalCond('carriers_on_deck > 0')) {
                    enabled.add('C0CW');
                } } catch (_ge) { /* guard not evaluable */ }
                enabled.add('C0CT');
                enabled.add('C0CS');
                enabled.add('C0CP');
                enabled.add('C0CB');
                enabled.add('C0CU');
                enabled.add('C0IV');
                enabled.add('C0CA');
                enabled.add('C0CN');
                enabled.add('C0DB');
                enabled.add('C0DR');
                enabled.add('C0RC');
                enabled.add('C0QA');
                enabled.add('C0CQ');
                enabled.add('C0VL');
                break;
            }
            case 'identifying': {
                enabled.add('identify.done');
                break;
            }
            case 'loading': {
                enabled.add('load.done');
                enabled.add('load.error');
                break;
            }
            case 'unloading': {
                enabled.add('unload.done');
                break;
            }
            case 'monitoring': {
                enabled.add('monitor.done');
                break;
            }
            case 'error_al': {
                enabled.add('C0II');
                break;
            }
            default: break;
        }
        return enabled;
    }


    /**
     * Add a trace listener for debugging/monitoring.
     * @param {Object} listener - Listener object with trace callback methods:
     *   - onSessionStart(sessionId, machineName, timestamp)
     *   - onSessionEnd(sessionId, finalStates, timestamp)
     *   - onStateEnter(stateId, activeStates, timestamp)
     *   - onStateExit(stateId, activeStates, timestamp)
     *   - onTransitionExecute(sourceId, targetIds, event, cond, timestamp)
     *   - onEventReceived(eventName, eventType, timestamp)
     *   - onEventProcessed(eventName, timestamp)
     *   - onActionExecute(actionType, details, timestamp)
     */
    addTraceListener(listener) {
        this._traceListener = listener;
    }

}

// CommonJS export for Node.js compatibility
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { AutoLoadSM };
}
