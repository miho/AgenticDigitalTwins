/**
 * Generated SCXML State Machine: HeaterShakerSM
 * 
 * This file was automatically generated by scxml-gen.
 * Do not edit manually.
 * 
 * @see https://www.w3.org/TR/scxml/
 */

import { ScxmlStateMachine } from './scxml-runtime.js';

export class HeaterShakerSM extends ScxmlStateMachine {
    // State IDs
    static S_OPERATIONAL = 'operational';
    static S_NOT_INITIALIZED = 'not_initialized';
    static S_IDLE_HHS = 'idle_hhs';
    static S_HEATING = 'heating';
    static S_WAITING_TEMP = 'waiting_temp';
    static S_SHAKING_STATE = 'shaking_state';
    static S_ERROR_HHS = 'error_hhs';

    // Event IDs for optimized dispatch
    // Use these constants with sendById() for maximum performance
    static EVT_T1LS = 15;
    static EVT_T1TW = 4;
    static EVT_T1LO = 14;
    static EVT_T1LP = 13;
    static EVT_T1QC = 20;
    static EVT_T1SB = 10;
    static EVT_T1RA = 16;
    static EVT_T1SC = 11;
    static EVT_T1TA = 3;
    static EVT_T1SA = 6;
    static EVT_T1LA = 12;
    static EVT_HHS_TEMP_REACHED = 23;
    static EVT_T1SI = 1;
    static EVT_T1QD = 21;
    static EVT_T1RF = 18;
    static EVT_T1QE = 22;
    static EVT_T1ST = 9;
    static EVT_T1RT = 19;
    static EVT_T1RQ = 17;
    static EVT_T1SS = 7;
    static EVT_T1LI = 2;
    static EVT_T1TO = 5;
    static EVT_HHS_TEMP_ERROR = 24;
    static EVT_T1SO = 8;
    static EVT_UNKNOWN = 9999;

    // Event name to ID mapping for string-to-int conversion
    static EVENT_MAP = new Map([['T1LS', 15], ['T1TW', 4], ['T1LO', 14], ['T1LP', 13], ['T1QC', 20], ['T1SB', 10], ['T1RA', 16], ['T1SC', 11], ['T1TA', 3], ['T1SA', 6], ['T1LA', 12], ['hhs_temp.reached', 23], ['T1SI', 1], ['T1QD', 21], ['T1RF', 18], ['T1QE', 22], ['T1ST', 9], ['T1RT', 19], ['T1RQ', 17], ['T1SS', 7], ['T1LI', 2], ['T1TO', 5], ['hhs_temp.error', 24], ['T1SO', 8]]);

    constructor() {
        super();
        this._name = 'HamiltonSTAR_HeaterShaker';
        this._traceListener = null;
        // Pre-allocated Sets for dispatch (avoids allocation per event)
        this._dispatchSnapshot = new Set();
        this._dispatchAdded = new Set();

        // Data model initialization
        try {
            this._datamodel['target_temp_01c'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['target_temp_01c'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['current_temp_01c'] = this._evalExpr('250', true);
        } catch (__e) {
            this._datamodel['current_temp_01c'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['shaking'] = this._evalExpr('false', true);
        } catch (__e) {
            this._datamodel['shaking'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['shake_speed'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['shake_speed'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['plate_locked'] = this._evalExpr('false', true);
        } catch (__e) {
            this._datamodel['plate_locked'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['temp_active'] = this._evalExpr('false', true);
        } catch (__e) {
            this._datamodel['temp_active'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['last_error'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['last_error'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
    }

    _enterInitialConfiguration() {
        if (this._traceListener) this._traceListener.onSessionStart(this._sessionid, 'HamiltonSTAR_HeaterShaker', Date.now());
        this._enter_operational();
        if (!this.activeStates.has('operational')) {
            this._enter_operational();
        }
        this._enter_not_initialized();
        this._checkEventlessTransitions();
    }

    _getStateChildren(stateId) {
        const children = {
            'operational': ['not_initialized', 'idle_hhs', 'heating', 'waiting_temp', 'shaking_state', 'error_hhs'],
        };
        return children[stateId] || [];
    }

    _getStateParent(stateId) {
        const parents = {
            'not_initialized': 'operational',
            'idle_hhs': 'operational',
            'heating': 'operational',
            'waiting_temp': 'operational',
            'shaking_state': 'operational',
            'error_hhs': 'operational',
        };
        return parents[stateId] || null;
    }

    _getStateOrder(stateId) {
        const order = {
            'operational': 0,
            'not_initialized': 1,
            'idle_hhs': 2,
            'heating': 3,
            'waiting_temp': 4,
            'shaking_state': 5,
            'error_hhs': 6,
        };
        return order[stateId] !== undefined ? order[stateId] : 9999;
    }

    _getEnabledTransitionForState(stateId, event) {
        if (stateId === 'not_initialized') {
            return this._getEnabledTransition_not_initialized(event);
        } else if (stateId === 'idle_hhs') {
            return this._getEnabledTransition_idle_hhs(event);
        } else if (stateId === 'heating') {
            return this._getEnabledTransition_heating(event);
        } else if (stateId === 'waiting_temp') {
            return this._getEnabledTransition_waiting_temp(event);
        } else if (stateId === 'shaking_state') {
            return this._getEnabledTransition_shaking_state(event);
        } else if (stateId === 'error_hhs') {
            return this._getEnabledTransition_error_hhs(event);
        }
        return null;
    }

    _checkEventlessTransitions() {
        let changed = true;
        let iterations = 0;
        const maxIterations = 1000;

        while (changed && iterations < maxIterations) {
            changed = false;
            iterations++;

            // For each atomic state, find first matching eventless transition in lineage
            const enabledEventless = [];
            const addedTransitions = new Set();

            // Helper: get eventless transition for a state
            const getEventless = (stateId) => {
                return null;
            };

            // Find atomic states
            const atomicStates = [];
            for (const stateId of this.activeStates) {
                const children = this._getStateChildren(stateId);
                let hasActiveChild = false;
                for (const childId of children) {
                    if (this.activeStates.has(childId)) { hasActiveChild = true; break; }
                }
                if (!hasActiveChild) atomicStates.push(stateId);
            }

            // For each atomic state, traverse lineage and pick first matching
            atomicStates.sort((a, b) => this._getStateOrder(a) - this._getStateOrder(b));
            for (const atomicState of atomicStates) {
                let currentState = atomicState;
                while (currentState) {
                    const t = getEventless(currentState);
                    if (t) {
                        const key = t.source + ':' + t.priority;
                        if (!addedTransitions.has(key)) {
                            addedTransitions.add(key);
                            enabledEventless.push(t);
                        }
                        break;
                    }
                    currentState = this._getStateParent(currentState);
                }
            }

            // Filter conflicting and execute microstep
            if (enabledEventless.length > 0) {
                enabledEventless.sort((a, b) => a.priority - b.priority);
                const filtered = this._filterConflictingTransitions(enabledEventless);
                if (filtered.length > 0) {
                    changed = true;
                    // Exit in REVERSE document order
                    const sortedByReverse = [...filtered].sort((a, b) => b.priority - a.priority);
                    for (const t of sortedByReverse) t.exit();
                    // Content and enter in document order
                    filtered.sort((a, b) => a.priority - b.priority);
                    for (const t of filtered) t.content();
                    for (const t of filtered) t.enter();
                }
            }
        }
    }

    _enter_operational() {
        this._enterState('operational');
        if (this._traceListener) this._traceListener.onStateEnter('operational', [...this.activeStates], Date.now());
    }

    _exit_operational() {
        if (this.activeStates.has('error_hhs')) {
            this._exit_error_hhs();
        }
        if (this.activeStates.has('shaking_state')) {
            this._exit_shaking_state();
        }
        if (this.activeStates.has('waiting_temp')) {
            this._exit_waiting_temp();
        }
        if (this.activeStates.has('heating')) {
            this._exit_heating();
        }
        if (this.activeStates.has('idle_hhs')) {
            this._exit_idle_hhs();
        }
        if (this.activeStates.has('not_initialized')) {
            this._exit_not_initialized();
        }
        if (this._traceListener) this._traceListener.onStateExit('operational', [...this.activeStates], Date.now());
        this._exitState('operational');
    }

    _enter_not_initialized() {
        this._enterState('not_initialized');
        if (this._traceListener) this._traceListener.onStateEnter('not_initialized', [...this.activeStates], Date.now());
    }

    _exit_not_initialized() {
        if (this._traceListener) this._traceListener.onStateExit('not_initialized', [...this.activeStates], Date.now());
        this._exitState('not_initialized');
    }

    _getEnabledTransition_not_initialized(event) {
        if (this._matchEvent(event.name, 'T1SI')) {
            return {
                source: 'not_initialized',
                priority: 1,
                exitSet: new Set(['not_initialized']),
                execute: () => {
                    if (this.activeStates.has('not_initialized')) {
                        this._exit_not_initialized();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('not_initialized', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shaking', expr: 'false'}, Date.now());
                    this._validateAndAssign('shaking', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'temp_active', expr: 'false'}, Date.now());
                    this._validateAndAssign('temp_active', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'plate_locked', expr: 'false'}, Date.now());
                    this._validateAndAssign('plate_locked', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '0'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Shaker initialized\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Shaker initialized\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1LI')) {
            return {
                source: 'not_initialized',
                priority: 1,
                exitSet: new Set(['not_initialized']),
                execute: () => {
                    if (this.activeStates.has('not_initialized')) {
                        this._exit_not_initialized();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('not_initialized', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'plate_locked', expr: 'false'}, Date.now());
                    this._validateAndAssign('plate_locked', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '0'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Lock initialized\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Lock initialized\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        return null;
    }

    _enter_idle_hhs() {
        this._enterState('idle_hhs');
        if (this._traceListener) this._traceListener.onStateEnter('idle_hhs', [...this.activeStates], Date.now());
    }

    _exit_idle_hhs() {
        if (this._traceListener) this._traceListener.onStateExit('idle_hhs', [...this.activeStates], Date.now());
        this._exitState('idle_hhs');
    }

    _getEnabledTransition_idle_hhs(event) {
        if (this._matchEvent(event.name, 'T1TA')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['heating'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_temp_01c', expr: '_event.data.ta||0'}, Date.now());
                    this._validateAndAssign('target_temp_01c', this._evalExpr('_event.data.ta||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'temp_active', expr: 'true'}, Date.now());
                    this._validateAndAssign('temp_active', this._evalExpr('true', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Set temp target=\'+((_event.data.ta||0)/10)+\'C\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Set temp target=\'+((_event.data.ta||0)/10)+\'C\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_heating();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1TW')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['waiting_temp'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_temp_01c', expr: '_event.data.ta||target_temp_01c'}, Date.now());
                    this._validateAndAssign('target_temp_01c', this._evalExpr('_event.data.ta||target_temp_01c', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'temp_active', expr: 'true'}, Date.now());
                    this._validateAndAssign('temp_active', this._evalExpr('true', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Wait for temp=\'+((_event.data.ta||target_temp_01c)/10)+\'C\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Wait for temp=\'+((_event.data.ta||target_temp_01c)/10)+\'C\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_waiting_temp();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1TO')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'temp_active', expr: 'false'}, Date.now());
                    this._validateAndAssign('temp_active', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_temp_01c', expr: '0'}, Date.now());
                    this._validateAndAssign('target_temp_01c', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temperature off\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temperature off\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1SA')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['shaking_state'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shaking', expr: 'true'}, Date.now());
                    this._validateAndAssign('shaking', this._evalExpr('true', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shake_speed', expr: '_event.data.sv||0'}, Date.now());
                    this._validateAndAssign('shake_speed', this._evalExpr('_event.data.sv||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Start shaking speed=\'+(_event.data.sv||0)'}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Start shaking speed=\'+(_event.data.sv||0)'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_shaking_state();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1SS')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shaking', expr: 'false'}, Date.now());
                    this._validateAndAssign('shaking', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shake_speed', expr: '0'}, Date.now());
                    this._validateAndAssign('shake_speed', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Stop shaking\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Stop shaking\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1SO')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Shaker params set\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Shaker params set\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1ST')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Shaker time set\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Shaker time set\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1SB')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Shaker accel set\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Shaker accel set\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1SC')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Shaker decel set\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Shaker decel set\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1LA')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'plate_locked', expr: 'true'}, Date.now());
                    this._validateAndAssign('plate_locked', this._evalExpr('true', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Plate locked\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Plate locked\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1LP')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'plate_locked', expr: 'true'}, Date.now());
                    this._validateAndAssign('plate_locked', this._evalExpr('true', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Plate lock activated\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Plate lock activated\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1LO')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'plate_locked', expr: 'false'}, Date.now());
                    this._validateAndAssign('plate_locked', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Plate unlocked\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Plate unlocked\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1LS')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Lock status: \'+(plate_locked?\'locked\':\'open\')'}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Lock status: \'+(plate_locked?\'locked\':\'open\')'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1RA')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Status — temp=\'+(current_temp_01c/10)+\'C shaking=\'+shaking+\' locked=\'+plate_locked'}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Status — temp=\'+(current_temp_01c/10)+\'C shaking=\'+shaking+\' locked=\'+plate_locked'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1RQ')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Module status OK\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Module status OK\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1RF')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Firmware version 1.0\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Firmware version 1.0\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1RT')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temp=\'+(current_temp_01c/10)+\'C target=\'+(target_temp_01c/10)+\'C\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temp=\'+(current_temp_01c/10)+\'C target=\'+(target_temp_01c/10)+\'C\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1QC')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Current temp=\'+(current_temp_01c/10)+\'C\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Current temp=\'+(current_temp_01c/10)+\'C\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1QD')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Target temp=\'+(target_temp_01c/10)+\'C\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Target temp=\'+(target_temp_01c/10)+\'C\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1QE')) {
            return {
                source: 'idle_hhs',
                priority: 2,
                exitSet: new Set(['idle_hhs']),
                execute: () => {
                    if (this.activeStates.has('idle_hhs')) {
                        this._exit_idle_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle_hhs', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temp active=\'+temp_active'}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temp active=\'+temp_active'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        return null;
    }

    _enter_heating() {
        this._enterState('heating');
        if (this._traceListener) this._traceListener.onStateEnter('heating', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('send', {event: 'hhs_temp.reached', target: '', delay: 'expr=_event.data._delay || \'5000ms\''}, Date.now());
            this._send('hhs_temp.reached', {delay: this._evalExpr('_event.data._delay || \'5000ms\''), id: 'heat_timer'});
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_heating() {
        if (this._traceListener) this._traceListener.onStateExit('heating', [...this.activeStates], Date.now());
        this._exitState('heating');
    }

    _getEnabledTransition_heating(event) {
        if (this._matchEvent(event.name, 'hhs_temp.reached')) {
            return {
                source: 'heating',
                priority: 3,
                exitSet: new Set(['heating']),
                execute: () => {
                    if (this.activeStates.has('heating')) {
                        this._exit_heating();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('heating', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'current_temp_01c', expr: 'target_temp_01c'}, Date.now());
                    this._validateAndAssign('current_temp_01c', this._evalExpr('target_temp_01c', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temperature reached \'+(target_temp_01c/10)+\'C\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temperature reached \'+(target_temp_01c/10)+\'C\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1TO')) {
            return {
                source: 'heating',
                priority: 3,
                exitSet: new Set(['heating']),
                execute: () => {
                    if (this.activeStates.has('heating')) {
                        this._exit_heating();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('heating', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'temp_active', expr: 'false'}, Date.now());
                    this._validateAndAssign('temp_active', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_temp_01c', expr: '0'}, Date.now());
                    this._validateAndAssign('target_temp_01c', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temperature off\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temperature off\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1SA')) {
            return {
                source: 'heating',
                priority: 3,
                exitSet: new Set(['heating']),
                execute: () => {
                    if (this.activeStates.has('heating')) {
                        this._exit_heating();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('heating', ['heating'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shaking', expr: 'true'}, Date.now());
                    this._validateAndAssign('shaking', this._evalExpr('true', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shake_speed', expr: '_event.data.sv||0'}, Date.now());
                    this._validateAndAssign('shake_speed', this._evalExpr('_event.data.sv||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Start shaking while heating\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Start shaking while heating\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_heating();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1SS')) {
            return {
                source: 'heating',
                priority: 3,
                exitSet: new Set(['heating']),
                execute: () => {
                    if (this.activeStates.has('heating')) {
                        this._exit_heating();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('heating', ['heating'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shaking', expr: 'false'}, Date.now());
                    this._validateAndAssign('shaking', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Stop shaking\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Stop shaking\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_heating();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1RT')) {
            return {
                source: 'heating',
                priority: 3,
                exitSet: new Set(['heating']),
                execute: () => {
                    if (this.activeStates.has('heating')) {
                        this._exit_heating();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('heating', ['heating'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temp=\'+(current_temp_01c/10)+\'C target=\'+(target_temp_01c/10)+\'C (heating)\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temp=\'+(current_temp_01c/10)+\'C target=\'+(target_temp_01c/10)+\'C (heating)\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_heating();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1QC')) {
            return {
                source: 'heating',
                priority: 3,
                exitSet: new Set(['heating']),
                execute: () => {
                    if (this.activeStates.has('heating')) {
                        this._exit_heating();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('heating', ['heating'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Current temp=\'+(current_temp_01c/10)+\'C\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Current temp=\'+(current_temp_01c/10)+\'C\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_heating();
                }
            };
        }
        return null;
    }

    _enter_waiting_temp() {
        this._enterState('waiting_temp');
        if (this._traceListener) this._traceListener.onStateEnter('waiting_temp', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('send', {event: 'hhs_temp.reached', target: '', delay: 'expr=_event.data._delay || \'10000ms\''}, Date.now());
            this._send('hhs_temp.reached', {delay: this._evalExpr('_event.data._delay || \'10000ms\''), id: 'wait_temp_timer'});
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_waiting_temp() {
        if (this._traceListener) this._traceListener.onStateExit('waiting_temp', [...this.activeStates], Date.now());
        this._exitState('waiting_temp');
    }

    _getEnabledTransition_waiting_temp(event) {
        if (this._matchEvent(event.name, 'hhs_temp.reached')) {
            return {
                source: 'waiting_temp',
                priority: 4,
                exitSet: new Set(['waiting_temp']),
                execute: () => {
                    if (this.activeStates.has('waiting_temp')) {
                        this._exit_waiting_temp();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('waiting_temp', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'current_temp_01c', expr: 'target_temp_01c'}, Date.now());
                    this._validateAndAssign('current_temp_01c', this._evalExpr('target_temp_01c', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temperature reached \'+(target_temp_01c/10)+\'C\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temperature reached \'+(target_temp_01c/10)+\'C\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1TO')) {
            return {
                source: 'waiting_temp',
                priority: 4,
                exitSet: new Set(['waiting_temp']),
                execute: () => {
                    if (this.activeStates.has('waiting_temp')) {
                        this._exit_waiting_temp();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('waiting_temp', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'temp_active', expr: 'false'}, Date.now());
                    this._validateAndAssign('temp_active', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_temp_01c', expr: '0'}, Date.now());
                    this._validateAndAssign('target_temp_01c', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temperature off\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temperature off\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'hhs_temp.error')) {
            return {
                source: 'waiting_temp',
                priority: 4,
                exitSet: new Set(['waiting_temp']),
                execute: () => {
                    if (this.activeStates.has('waiting_temp')) {
                        this._exit_waiting_temp();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('waiting_temp', ['error_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '99'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('99', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'HHS: Temperature timeout\''}, Date.now());
                    this._log('ERROR', this._evalExpr('\'HHS: Temperature timeout\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_error_hhs();
                }
            };
        }
        return null;
    }

    _enter_shaking_state() {
        this._enterState('shaking_state');
        if (this._traceListener) this._traceListener.onStateEnter('shaking_state', [...this.activeStates], Date.now());
    }

    _exit_shaking_state() {
        if (this._traceListener) this._traceListener.onStateExit('shaking_state', [...this.activeStates], Date.now());
        this._exitState('shaking_state');
    }

    _getEnabledTransition_shaking_state(event) {
        if (this._matchEvent(event.name, 'T1SS')) {
            return {
                source: 'shaking_state',
                priority: 5,
                exitSet: new Set(['shaking_state']),
                execute: () => {
                    if (this.activeStates.has('shaking_state')) {
                        this._exit_shaking_state();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('shaking_state', ['idle_hhs'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shaking', expr: 'false'}, Date.now());
                    this._validateAndAssign('shaking', this._evalExpr('false', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'shake_speed', expr: '0'}, Date.now());
                    this._validateAndAssign('shake_speed', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Shaking stopped\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Shaking stopped\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle_hhs();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1TA')) {
            return {
                source: 'shaking_state',
                priority: 5,
                exitSet: new Set(['shaking_state']),
                execute: () => {
                    if (this.activeStates.has('shaking_state')) {
                        this._exit_shaking_state();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('shaking_state', ['shaking_state'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'target_temp_01c', expr: '_event.data.ta||0'}, Date.now());
                    this._validateAndAssign('target_temp_01c', this._evalExpr('_event.data.ta||0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'temp_active', expr: 'true'}, Date.now());
                    this._validateAndAssign('temp_active', this._evalExpr('true', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Set temp while shaking\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Set temp while shaking\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_shaking_state();
                }
            };
        }
        if (this._matchEvent(event.name, 'T1RT')) {
            return {
                source: 'shaking_state',
                priority: 5,
                exitSet: new Set(['shaking_state']),
                execute: () => {
                    if (this.activeStates.has('shaking_state')) {
                        this._exit_shaking_state();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('shaking_state', ['shaking_state'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'HHS: Temp=\'+(current_temp_01c/10)+\'C (shaking at \'+shake_speed+\')\''}, Date.now());
                    this._log('FW', this._evalExpr('\'HHS: Temp=\'+(current_temp_01c/10)+\'C (shaking at \'+shake_speed+\')\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_shaking_state();
                }
            };
        }
        return null;
    }

    _enter_error_hhs() {
        this._enterState('error_hhs');
        if (this._traceListener) this._traceListener.onStateEnter('error_hhs', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'HHS error \'+last_error'}, Date.now());
            this._log('ERROR', this._evalExpr('\'HHS error \'+last_error'));
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_error_hhs() {
        if (this._traceListener) this._traceListener.onStateExit('error_hhs', [...this.activeStates], Date.now());
        this._exitState('error_hhs');
    }

    _getEnabledTransition_error_hhs(event) {
        if (this._matchEvent(event.name, 'T1SI')) {
            return {
                source: 'error_hhs',
                priority: 6,
                exitSet: new Set(['error_hhs']),
                execute: () => {
                    if (this.activeStates.has('error_hhs')) {
                        this._exit_error_hhs();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('error_hhs', ['not_initialized'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '0'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('0', true));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_not_initialized();
                }
            };
        }
        return null;
    }

    /**
     * Get the event ID for an event name.
     * @param {string} eventName - The event name
     * @returns {number} The event ID or EVT_UNKNOWN (9999) if not found
     */
    getEventId(eventName) {
        return this.constructor.EVENT_MAP.get(eventName) ?? this.constructor.EVT_UNKNOWN;
    }

    /**
     * Get the event name for an event ID.
     * @param {number} eventId - The event ID
     * @returns {string|null} The event name or null if unknown
     */
    getEventName(eventId) {
        switch (eventId) {
            case 15: return 'T1LS';
            case 4: return 'T1TW';
            case 14: return 'T1LO';
            case 13: return 'T1LP';
            case 20: return 'T1QC';
            case 10: return 'T1SB';
            case 16: return 'T1RA';
            case 11: return 'T1SC';
            case 3: return 'T1TA';
            case 6: return 'T1SA';
            case 12: return 'T1LA';
            case 23: return 'hhs_temp.reached';
            case 1: return 'T1SI';
            case 21: return 'T1QD';
            case 18: return 'T1RF';
            case 22: return 'T1QE';
            case 9: return 'T1ST';
            case 19: return 'T1RT';
            case 17: return 'T1RQ';
            case 7: return 'T1SS';
            case 2: return 'T1LI';
            case 5: return 'T1TO';
            case 24: return 'hhs_temp.error';
            case 8: return 'T1SO';
            default: return null;
        }
    }

    /**
     * Send an event using its integer ID for optimized O(1) dispatch.
     * This bypasses string-to-ID lookup for maximum performance.
     * @param {number} eventId - The event ID (use EVT_* constants)
     * @param {*} [data=null] - Optional event data
     */
    sendById(eventId, data = null) {
        if (this._finished) return;
        const eventName = this.getEventName(eventId);
        if (eventName === null) {
            console.warn('Unknown event ID:', eventId);
            return;
        }
        this.send(eventName, data);
    }

    _matchEvent(eventName, pattern) {
        if (pattern === '*' || pattern === '.*') return true;
        if (pattern === eventName) return true;

        // Prefix matching: 'foo' matches 'foo.bar'
        if (eventName.startsWith(pattern + '.')) return true;

        // Handle space-separated patterns
        const patterns = pattern.split(/\s+/);
        for (const p of patterns) {
            if (p === '*' || p === '.*') return true;
            if (p === eventName) return true;
            if (p.endsWith('.*') && eventName.startsWith(p.slice(0, -2))) return true;
            if (eventName.startsWith(p + '.')) return true;
        }

        return false;
    }

    /**
     * Check if an event ID matches a pattern (internal use).
     * Uses integer comparison for known events, falls back to string for unknown.
     */
    _matchEventById(eventId, eventName, patternId, pattern) {
        // Fast path: wildcard '*' matches everything
        if (pattern === '*' || pattern === '.*') return true;

        // Fast path: exact match by ID (for known events)
        if (eventId !== this.constructor.EVT_UNKNOWN && eventId === patternId) return true;

        // For unknown events or prefix matching, fall back to string comparison
        return this._matchEvent(eventName, pattern);
    }


    static _ALL_EVENTS = new Set(['T1SI', 'T1LI', 'T1TA', 'T1TW', 'T1TO', 'T1SA', 'T1SS', 'T1SO', 'T1ST', 'T1SB', 'T1SC', 'T1LA', 'T1LP', 'T1LO', 'T1LS', 'T1RA', 'T1RQ', 'T1RF', 'T1RT', 'T1QC', 'T1QD', 'T1QE', 'hhs_temp.reached', 'hhs_temp.error']);

    /**
     * Returns all events this state machine can react to.
     * @returns {Set<string>}
     */
    getAllEvents() {
        return new Set(this.constructor._ALL_EVENTS);
    }

    /**
     * Returns events currently enabled based on active states and guard conditions.
     * @returns {Set<string>}
     */
    getEnabledEvents() {
        const enabled = new Set();
        if (this.activeStates.has('not_initialized')) {
            enabled.add('T1SI');
            enabled.add('T1LI');
        }
        if (this.activeStates.has('idle_hhs')) {
            enabled.add('T1TA');
            enabled.add('T1TW');
            enabled.add('T1TO');
            enabled.add('T1SA');
            enabled.add('T1SS');
            enabled.add('T1SO');
            enabled.add('T1ST');
            enabled.add('T1SB');
            enabled.add('T1SC');
            enabled.add('T1LA');
            enabled.add('T1LP');
            enabled.add('T1LO');
            enabled.add('T1LS');
            enabled.add('T1RA');
            enabled.add('T1RQ');
            enabled.add('T1RF');
            enabled.add('T1RT');
            enabled.add('T1QC');
            enabled.add('T1QD');
            enabled.add('T1QE');
        }
        if (this.activeStates.has('heating')) {
            enabled.add('hhs_temp.reached');
            enabled.add('T1TO');
            enabled.add('T1SA');
            enabled.add('T1SS');
            enabled.add('T1RT');
            enabled.add('T1QC');
        }
        if (this.activeStates.has('waiting_temp')) {
            enabled.add('hhs_temp.reached');
            enabled.add('T1TO');
            enabled.add('hhs_temp.error');
        }
        if (this.activeStates.has('shaking_state')) {
            enabled.add('T1SS');
            enabled.add('T1TA');
            enabled.add('T1RT');
        }
        if (this.activeStates.has('error_hhs')) {
            enabled.add('T1SI');
        }
        return enabled;
    }

    /**
     * Returns all events a specific state can react to (including ancestor transitions).
     * @param {string} stateId
     * @returns {Set<string>}
     */
    getEventsForState(stateId) {
        switch (stateId) {
            case 'not_initialized': return new Set(['T1SI', 'T1LI']);
            case 'idle_hhs': return new Set(['T1TA', 'T1TW', 'T1TO', 'T1SA', 'T1SS', 'T1SO', 'T1ST', 'T1SB', 'T1SC', 'T1LA', 'T1LP', 'T1LO', 'T1LS', 'T1RA', 'T1RQ', 'T1RF', 'T1RT', 'T1QC', 'T1QD', 'T1QE']);
            case 'heating': return new Set(['hhs_temp.reached', 'T1TO', 'T1SA', 'T1SS', 'T1RT', 'T1QC']);
            case 'waiting_temp': return new Set(['hhs_temp.reached', 'T1TO', 'hhs_temp.error']);
            case 'shaking_state': return new Set(['T1SS', 'T1TA', 'T1RT']);
            case 'error_hhs': return new Set(['T1SI']);
            default: return new Set();
        }
    }

    /**
     * Returns events currently enabled for a specific active state (guard-aware).
     * @param {string} stateId
     * @returns {Set<string>}
     */
    getEnabledEventsForState(stateId) {
        if (!this.activeStates.has(stateId)) return new Set();
        const enabled = new Set();
        switch (stateId) {
            case 'not_initialized': {
                enabled.add('T1SI');
                enabled.add('T1LI');
                break;
            }
            case 'idle_hhs': {
                enabled.add('T1TA');
                enabled.add('T1TW');
                enabled.add('T1TO');
                enabled.add('T1SA');
                enabled.add('T1SS');
                enabled.add('T1SO');
                enabled.add('T1ST');
                enabled.add('T1SB');
                enabled.add('T1SC');
                enabled.add('T1LA');
                enabled.add('T1LP');
                enabled.add('T1LO');
                enabled.add('T1LS');
                enabled.add('T1RA');
                enabled.add('T1RQ');
                enabled.add('T1RF');
                enabled.add('T1RT');
                enabled.add('T1QC');
                enabled.add('T1QD');
                enabled.add('T1QE');
                break;
            }
            case 'heating': {
                enabled.add('hhs_temp.reached');
                enabled.add('T1TO');
                enabled.add('T1SA');
                enabled.add('T1SS');
                enabled.add('T1RT');
                enabled.add('T1QC');
                break;
            }
            case 'waiting_temp': {
                enabled.add('hhs_temp.reached');
                enabled.add('T1TO');
                enabled.add('hhs_temp.error');
                break;
            }
            case 'shaking_state': {
                enabled.add('T1SS');
                enabled.add('T1TA');
                enabled.add('T1RT');
                break;
            }
            case 'error_hhs': {
                enabled.add('T1SI');
                break;
            }
            default: break;
        }
        return enabled;
    }


    /**
     * Add a trace listener for debugging/monitoring.
     * @param {Object} listener - Listener object with trace callback methods:
     *   - onSessionStart(sessionId, machineName, timestamp)
     *   - onSessionEnd(sessionId, finalStates, timestamp)
     *   - onStateEnter(stateId, activeStates, timestamp)
     *   - onStateExit(stateId, activeStates, timestamp)
     *   - onTransitionExecute(sourceId, targetIds, event, cond, timestamp)
     *   - onEventReceived(eventName, eventType, timestamp)
     *   - onEventProcessed(eventName, timestamp)
     *   - onActionExecute(actionType, details, timestamp)
     */
    addTraceListener(listener) {
        this._traceListener = listener;
    }

}

// CommonJS export for Node.js compatibility
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { HeaterShakerSM };
}
