/**
 * Generated SCXML State Machine: PipChannelSM
 * 
 * This file was automatically generated by scxml-gen.
 * Do not edit manually.
 * 
 * @see https://www.w3.org/TR/scxml/
 */

import { ScxmlStateMachine } from './scxml-runtime.js';

export class PipChannelSM extends ScxmlStateMachine {
    // State IDs
    static S_OPERATIONAL = 'operational';
    static S_NOT_INITIALIZED = 'not_initialized';
    static S_IDLE = 'idle';
    static S_NO_TIP = 'no_tip';
    static S_TIP_FITTED = 'tip_fitted';
    static S_TIP_EMPTY = 'tip_empty';
    static S_TIP_LOADED = 'tip_loaded';
    static S_MOVING = 'moving';
    static S_WASHING = 'washing';
    static S_ERROR = 'error';

    // Event IDs for optimized dispatch
    // Use these constants with sendById() for maximum performance
    static EVT_C0AS = 5;
    static EVT_C0DF = 9;
    static EVT_C0DS = 6;
    static EVT_C0TR = 8;
    static EVT_C0TP = 4;
    static EVT_C0LW = 7;
    static EVT_MOVE_DONE = 10;
    static EVT_C0ZA = 3;
    static EVT_WASH_DONE = 11;
    static EVT_C0DI = 1;
    static EVT_C0JM = 2;
    static EVT_UNKNOWN = 9999;

    // Event name to ID mapping for string-to-int conversion
    static EVENT_MAP = new Map([['C0AS', 5], ['C0DF', 9], ['C0DS', 6], ['C0TR', 8], ['C0TP', 4], ['C0LW', 7], ['move.done', 10], ['C0ZA', 3], ['wash.done', 11], ['C0DI', 1], ['C0JM', 2]]);

    constructor() {
        super();
        this._name = 'HamiltonSTAR_PipChannel';
        this._traceListener = null;
        // Pre-allocated Sets for dispatch (avoids allocation per event)
        this._dispatchSnapshot = new Set();
        this._dispatchAdded = new Set();

        // Data model initialization
        try {
            this._datamodel['tip_pattern'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['tip_pattern'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['tip_type'] = this._evalExpr('-1', true);
        } catch (__e) {
            this._datamodel['tip_type'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['volume_held_01ul'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['volume_held_01ul'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['pos_x'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['pos_x'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['pos_y'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['pos_y'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['pos_z'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['pos_z'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['last_lld_01mm'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['last_lld_01mm'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
        try {
            this._datamodel['last_error'] = this._evalExpr('0', true);
        } catch (__e) {
            this._datamodel['last_error'] = undefined;
            this._initErrors = this._initErrors || [];
            this._initErrors.push('error.execution');
        }
    }

    _enterInitialConfiguration() {
        if (this._traceListener) this._traceListener.onSessionStart(this._sessionid, 'HamiltonSTAR_PipChannel', Date.now());
        this._enter_operational();
        if (!this.activeStates.has('operational')) {
            this._enter_operational();
        }
        this._enter_not_initialized();
        this._checkEventlessTransitions();
    }

    _getStateChildren(stateId) {
        const children = {
            'operational': ['not_initialized', 'idle', 'moving', 'washing', 'error'],
            'idle': ['no_tip', 'tip_fitted'],
            'tip_fitted': ['tip_empty', 'tip_loaded'],
        };
        return children[stateId] || [];
    }

    _getStateParent(stateId) {
        const parents = {
            'not_initialized': 'operational',
            'idle': 'operational',
            'no_tip': 'idle',
            'tip_fitted': 'idle',
            'tip_empty': 'tip_fitted',
            'tip_loaded': 'tip_fitted',
            'moving': 'operational',
            'washing': 'operational',
            'error': 'operational',
        };
        return parents[stateId] || null;
    }

    _getStateOrder(stateId) {
        const order = {
            'operational': 0,
            'not_initialized': 1,
            'idle': 2,
            'no_tip': 3,
            'tip_fitted': 4,
            'tip_empty': 5,
            'tip_loaded': 6,
            'moving': 7,
            'washing': 8,
            'error': 9,
        };
        return order[stateId] !== undefined ? order[stateId] : 9999;
    }

    _getEnabledTransitionForState(stateId, event) {
        if (stateId === 'not_initialized') {
            return this._getEnabledTransition_not_initialized(event);
        } else if (stateId === 'idle') {
            return this._getEnabledTransition_idle(event);
        } else if (stateId === 'no_tip') {
            return this._getEnabledTransition_no_tip(event);
        } else if (stateId === 'tip_fitted') {
            return this._getEnabledTransition_tip_fitted(event);
        } else if (stateId === 'tip_empty') {
            return this._getEnabledTransition_tip_empty(event);
        } else if (stateId === 'tip_loaded') {
            return this._getEnabledTransition_tip_loaded(event);
        } else if (stateId === 'moving') {
            return this._getEnabledTransition_moving(event);
        } else if (stateId === 'washing') {
            return this._getEnabledTransition_washing(event);
        } else if (stateId === 'error') {
            return this._getEnabledTransition_error(event);
        }
        return null;
    }

    _checkEventlessTransitions() {
        let changed = true;
        let iterations = 0;
        const maxIterations = 1000;

        while (changed && iterations < maxIterations) {
            changed = false;
            iterations++;

            // For each atomic state, find first matching eventless transition in lineage
            const enabledEventless = [];
            const addedTransitions = new Set();

            // Helper: get eventless transition for a state
            const getEventless = (stateId) => {
                return null;
            };

            // Find atomic states
            const atomicStates = [];
            for (const stateId of this.activeStates) {
                const children = this._getStateChildren(stateId);
                let hasActiveChild = false;
                for (const childId of children) {
                    if (this.activeStates.has(childId)) { hasActiveChild = true; break; }
                }
                if (!hasActiveChild) atomicStates.push(stateId);
            }

            // For each atomic state, traverse lineage and pick first matching
            atomicStates.sort((a, b) => this._getStateOrder(a) - this._getStateOrder(b));
            for (const atomicState of atomicStates) {
                let currentState = atomicState;
                while (currentState) {
                    const t = getEventless(currentState);
                    if (t) {
                        const key = t.source + ':' + t.priority;
                        if (!addedTransitions.has(key)) {
                            addedTransitions.add(key);
                            enabledEventless.push(t);
                        }
                        break;
                    }
                    currentState = this._getStateParent(currentState);
                }
            }

            // Filter conflicting and execute microstep
            if (enabledEventless.length > 0) {
                enabledEventless.sort((a, b) => a.priority - b.priority);
                const filtered = this._filterConflictingTransitions(enabledEventless);
                if (filtered.length > 0) {
                    changed = true;
                    // Exit in REVERSE document order
                    const sortedByReverse = [...filtered].sort((a, b) => b.priority - a.priority);
                    for (const t of sortedByReverse) t.exit();
                    // Content and enter in document order
                    filtered.sort((a, b) => a.priority - b.priority);
                    for (const t of filtered) t.content();
                    for (const t of filtered) t.enter();
                }
            }
        }
    }

    _enter_operational() {
        this._enterState('operational');
        if (this._traceListener) this._traceListener.onStateEnter('operational', [...this.activeStates], Date.now());
    }

    _exit_operational() {
        if (this.activeStates.has('error')) {
            this._exit_error();
        }
        if (this.activeStates.has('washing')) {
            this._exit_washing();
        }
        if (this.activeStates.has('moving')) {
            this._exit_moving();
        }
        if (this.activeStates.has('idle')) {
            this._exit_idle();
        }
        if (this.activeStates.has('not_initialized')) {
            this._exit_not_initialized();
        }
        if (this._traceListener) this._traceListener.onStateExit('operational', [...this.activeStates], Date.now());
        this._exitState('operational');
    }

    _enter_not_initialized() {
        this._enterState('not_initialized');
        if (this._traceListener) this._traceListener.onStateEnter('not_initialized', [...this.activeStates], Date.now());
    }

    _exit_not_initialized() {
        if (this._traceListener) this._traceListener.onStateExit('not_initialized', [...this.activeStates], Date.now());
        this._exitState('not_initialized');
    }

    _getEnabledTransition_not_initialized(event) {
        if (this._matchEvent(event.name, 'C0DI')) {
            return {
                source: 'not_initialized',
                priority: 1,
                exitSet: new Set(['not_initialized']),
                execute: () => {
                    if (this.activeStates.has('not_initialized')) {
                        this._exit_not_initialized();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('not_initialized', ['no_tip'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_pattern', expr: '0'}, Date.now());
                    this._validateAndAssign('tip_pattern', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_type', expr: '-1'}, Date.now());
                    this._validateAndAssign('tip_type', this._evalExpr('-1', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: '0'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Initialize PIP channels\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Initialize PIP channels\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    this._enter_no_tip();
                }
            };
        }
        return null;
    }

    _enter_idle() {
        this._enterState('idle');
        if (this._traceListener) this._traceListener.onStateEnter('idle', [...this.activeStates], Date.now());
    }

    _exit_idle() {
        if (this.activeStates.has('tip_fitted')) {
            this._exit_tip_fitted();
        }
        if (this.activeStates.has('no_tip')) {
            this._exit_no_tip();
        }
        if (this._traceListener) this._traceListener.onStateExit('idle', [...this.activeStates], Date.now());
        this._exitState('idle');
    }

    _getEnabledTransition_idle(event) {
        if (this._matchEvent(event.name, 'C0JM')) {
            return {
                source: 'idle',
                priority: 2,
                exitSet: new Set(['idle', 'no_tip', 'tip_fitted', 'tip_empty', 'tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('idle')) {
                        this._exit_idle();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle', ['moving'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_x', expr: '_event.data.xp'}, Date.now());
                    this._validateAndAssign('pos_x', this._evalExpr('_event.data.xp', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_y', expr: '_event.data.yp'}, Date.now());
                    this._validateAndAssign('pos_y', this._evalExpr('_event.data.yp', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_z', expr: '_event.data.zp'}, Date.now());
                    this._validateAndAssign('pos_z', this._evalExpr('_event.data.zp', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Move to X=\' + _event.data.xp'}, Date.now());
                    this._log('FW', this._evalExpr('\'Move to X=\' + _event.data.xp'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_moving();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0ZA')) {
            return {
                source: 'idle',
                priority: 2,
                exitSet: new Set(['idle', 'no_tip', 'tip_fitted', 'tip_empty', 'tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('idle')) {
                        this._exit_idle();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('idle', ['idle'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_z', expr: '0'}, Date.now());
                    this._validateAndAssign('pos_z', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Z-safety\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Z-safety\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle();
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    this._enter_no_tip();
                }
            };
        }
        return null;
    }

    _enter_no_tip() {
        this._enterState('no_tip');
        if (this._traceListener) this._traceListener.onStateEnter('no_tip', [...this.activeStates], Date.now());
    }

    _exit_no_tip() {
        if (this._traceListener) this._traceListener.onStateExit('no_tip', [...this.activeStates], Date.now());
        this._exitState('no_tip');
    }

    _getEnabledTransition_no_tip(event) {
        if (this._matchEvent(event.name, 'C0TP')) {
            return {
                source: 'no_tip',
                priority: 3,
                exitSet: new Set(['no_tip']),
                execute: () => {
                    if (this.activeStates.has('no_tip')) {
                        this._exit_no_tip();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('no_tip', ['tip_empty'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_pattern', expr: '_event.data.tm'}, Date.now());
                    this._validateAndAssign('tip_pattern', this._evalExpr('_event.data.tm', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_type', expr: '_event.data.tt'}, Date.now());
                    this._validateAndAssign('tip_type', this._evalExpr('_event.data.tt', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Tip pick-up type=\' + _event.data.tt'}, Date.now());
                    this._log('FW', this._evalExpr('\'Tip pick-up type=\' + _event.data.tt'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    if (!this.activeStates.has('tip_fitted')) {
                        this._enter_tip_fitted();
                    }
                    this._enter_tip_empty();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0AS')) {
            return {
                source: 'no_tip',
                priority: 3,
                exitSet: new Set(['no_tip', 'idle', 'tip_fitted', 'tip_empty', 'tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('idle')) {
                        this._exit_idle();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('no_tip', ['error'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '8'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('8', true));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_error();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0DS')) {
            return {
                source: 'no_tip',
                priority: 3,
                exitSet: new Set(['no_tip', 'idle', 'tip_fitted', 'tip_empty', 'tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('idle')) {
                        this._exit_idle();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('no_tip', ['error'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '8'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('8', true));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_error();
                }
            };
        }
        return null;
    }

    _enter_tip_fitted() {
        this._enterState('tip_fitted');
        if (this._traceListener) this._traceListener.onStateEnter('tip_fitted', [...this.activeStates], Date.now());
    }

    _exit_tip_fitted() {
        if (this.activeStates.has('tip_loaded')) {
            this._exit_tip_loaded();
        }
        if (this.activeStates.has('tip_empty')) {
            this._exit_tip_empty();
        }
        if (this._traceListener) this._traceListener.onStateExit('tip_fitted', [...this.activeStates], Date.now());
        this._exitState('tip_fitted');
    }

    _getEnabledTransition_tip_fitted(event) {
        if (this._matchEvent(event.name, 'C0LW')) {
            return {
                source: 'tip_fitted',
                priority: 4,
                exitSet: new Set(['tip_fitted', 'tip_empty', 'tip_loaded', 'idle', 'no_tip']),
                execute: () => {
                    if (this.activeStates.has('idle')) {
                        this._exit_idle();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_fitted', ['washing'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: '0'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'DC Wash\''}, Date.now());
                    this._log('FW', this._evalExpr('\'DC Wash\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_washing();
                }
            };
        }
        return null;
    }

    _enter_tip_empty() {
        this._enterState('tip_empty');
        if (this._traceListener) this._traceListener.onStateEnter('tip_empty', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: '0'}, Date.now());
            this._validateAndAssign('volume_held_01ul', this._evalExpr('0', true));
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_tip_empty() {
        if (this._traceListener) this._traceListener.onStateExit('tip_empty', [...this.activeStates], Date.now());
        this._exitState('tip_empty');
    }

    _getEnabledTransition_tip_empty(event) {
        if (this._matchEvent(event.name, 'C0AS')) {
            return {
                source: 'tip_empty',
                priority: 5,
                exitSet: new Set(['tip_empty']),
                execute: () => {
                    if (this.activeStates.has('tip_empty')) {
                        this._exit_tip_empty();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_empty', ['tip_loaded'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: '_event.data.av'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('_event.data.av', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'pos_x', expr: '_event.data.xp || pos_x'}, Date.now());
                    this._validateAndAssign('pos_x', this._evalExpr('_event.data.xp || pos_x', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Aspirate vol=\' + _event.data.av + \' (0.1ul)\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Aspirate vol=\' + _event.data.av + \' (0.1ul)\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    if (!this.activeStates.has('tip_fitted')) {
                        this._enter_tip_fitted();
                    }
                    this._enter_tip_loaded();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0TR')) {
            return {
                source: 'tip_empty',
                priority: 5,
                exitSet: new Set(['tip_empty', 'tip_fitted', 'tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('tip_fitted')) {
                        this._exit_tip_fitted();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_empty', ['no_tip'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_pattern', expr: '0'}, Date.now());
                    this._validateAndAssign('tip_pattern', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_type', expr: '-1'}, Date.now());
                    this._validateAndAssign('tip_type', this._evalExpr('-1', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Tip eject\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Tip eject\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    this._enter_no_tip();
                }
            };
        }
        return null;
    }

    _enter_tip_loaded() {
        this._enterState('tip_loaded');
        if (this._traceListener) this._traceListener.onStateEnter('tip_loaded', [...this.activeStates], Date.now());
    }

    _exit_tip_loaded() {
        if (this._traceListener) this._traceListener.onStateExit('tip_loaded', [...this.activeStates], Date.now());
        this._exitState('tip_loaded');
    }

    _getEnabledTransition_tip_loaded(event) {
        if (this._matchEvent(event.name, 'C0DS') && this._evalCond('_event.data.dm == 4')) {
            return {
                source: 'tip_loaded',
                priority: 6,
                exitSet: new Set(['tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('tip_loaded')) {
                        this._exit_tip_loaded();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_loaded', ['tip_loaded'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: '0'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Dispense empty-tip\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Dispense empty-tip\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    if (!this.activeStates.has('tip_fitted')) {
                        this._enter_tip_fitted();
                    }
                    this._enter_tip_loaded();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0DS') && this._evalCond('_event.data.dm == 2 || _event.data.dm == 3')) {
            return {
                source: 'tip_loaded',
                priority: 6,
                exitSet: new Set(['tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('tip_loaded')) {
                        this._exit_tip_loaded();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_loaded', ['tip_loaded'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: 'Math.max(0, volume_held_01ul - _event.data.dv)'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('Math.max(0, volume_held_01ul - _event.data.dv)', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Dispense surface partial vol=\' + _event.data.dv'}, Date.now());
                    this._log('FW', this._evalExpr('\'Dispense surface partial vol=\' + _event.data.dv'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    if (!this.activeStates.has('tip_fitted')) {
                        this._enter_tip_fitted();
                    }
                    this._enter_tip_loaded();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0DS')) {
            return {
                source: 'tip_loaded',
                priority: 6,
                exitSet: new Set(['tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('tip_loaded')) {
                        this._exit_tip_loaded();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_loaded', ['tip_empty'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: 'Math.max(0, volume_held_01ul - _event.data.dv)'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('Math.max(0, volume_held_01ul - _event.data.dv)', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Dispense jet vol=\' + _event.data.dv'}, Date.now());
                    this._log('FW', this._evalExpr('\'Dispense jet vol=\' + _event.data.dv'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    if (!this.activeStates.has('tip_fitted')) {
                        this._enter_tip_fitted();
                    }
                    this._enter_tip_empty();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0AS')) {
            return {
                source: 'tip_loaded',
                priority: 6,
                exitSet: new Set(['tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('tip_loaded')) {
                        this._exit_tip_loaded();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_loaded', ['tip_loaded'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: 'volume_held_01ul + _event.data.av'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('volume_held_01ul + _event.data.av', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Sequential aspirate +\' + _event.data.av'}, Date.now());
                    this._log('FW', this._evalExpr('\'Sequential aspirate +\' + _event.data.av'));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    if (!this.activeStates.has('tip_fitted')) {
                        this._enter_tip_fitted();
                    }
                    this._enter_tip_loaded();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0DF')) {
            return {
                source: 'tip_loaded',
                priority: 6,
                exitSet: new Set(['tip_loaded']),
                execute: () => {
                    if (this.activeStates.has('tip_loaded')) {
                        this._exit_tip_loaded();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_loaded', ['tip_loaded'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: 'Math.max(0, volume_held_01ul - (_event.data.dv * _event.data.xi))'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('Math.max(0, volume_held_01ul - (_event.data.dv * _event.data.xi))', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Dispense-on-fly \' + _event.data.xi + \' shoots\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Dispense-on-fly \' + _event.data.xi + \' shoots\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    if (!this.activeStates.has('tip_fitted')) {
                        this._enter_tip_fitted();
                    }
                    this._enter_tip_loaded();
                }
            };
        }
        if (this._matchEvent(event.name, 'C0TR')) {
            return {
                source: 'tip_loaded',
                priority: 6,
                exitSet: new Set(['tip_loaded', 'tip_fitted', 'tip_empty']),
                execute: () => {
                    if (this.activeStates.has('tip_fitted')) {
                        this._exit_tip_fitted();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('tip_loaded', ['no_tip'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'volume_held_01ul', expr: '0'}, Date.now());
                    this._validateAndAssign('volume_held_01ul', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_pattern', expr: '0'}, Date.now());
                    this._validateAndAssign('tip_pattern', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'tip_type', expr: '-1'}, Date.now());
                    this._validateAndAssign('tip_type', this._evalExpr('-1', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW_WARN', expr: '\'Tip eject with liquid held\''}, Date.now());
                    this._log('FW_WARN', this._evalExpr('\'Tip eject with liquid held\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    this._enter_no_tip();
                }
            };
        }
        return null;
    }

    _enter_moving() {
        this._enterState('moving');
        if (this._traceListener) this._traceListener.onStateEnter('moving', [...this.activeStates], Date.now());
    }

    _exit_moving() {
        if (this._traceListener) this._traceListener.onStateExit('moving', [...this.activeStates], Date.now());
        this._exitState('moving');
    }

    _getEnabledTransition_moving(event) {
        if (this._matchEvent(event.name, 'move.done')) {
            return {
                source: 'moving',
                priority: 7,
                exitSet: new Set(['moving']),
                execute: () => {
                    if (this.activeStates.has('moving')) {
                        this._exit_moving();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('moving', ['idle'], event.name, null, Date.now());
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_idle();
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    this._enter_no_tip();
                }
            };
        }
        return null;
    }

    _enter_washing() {
        this._enterState('washing');
        if (this._traceListener) this._traceListener.onStateEnter('washing', [...this.activeStates], Date.now());
    }

    _exit_washing() {
        if (this._traceListener) this._traceListener.onStateExit('washing', [...this.activeStates], Date.now());
        this._exitState('washing');
    }

    _getEnabledTransition_washing(event) {
        if (this._matchEvent(event.name, 'wash.done')) {
            return {
                source: 'washing',
                priority: 8,
                exitSet: new Set(['washing']),
                execute: () => {
                    if (this.activeStates.has('washing')) {
                        this._exit_washing();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('washing', ['tip_empty'], event.name, null, Date.now());
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    if (!this.activeStates.has('idle')) {
                        this._enter_idle();
                    }
                    if (!this.activeStates.has('tip_fitted')) {
                        this._enter_tip_fitted();
                    }
                    this._enter_tip_empty();
                }
            };
        }
        return null;
    }

    _enter_error() {
        this._enterState('error');
        if (this._traceListener) this._traceListener.onStateEnter('error', [...this.activeStates], Date.now());
        // onentry actions
        try {
            if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'ERROR', expr: '\'PIP error \' + last_error'}, Date.now());
            this._log('ERROR', this._evalExpr('\'PIP error \' + last_error'));
        } catch (__err) {
            if (__err.message && __err.message.startsWith('error.')) {
                this._raisePlatform(__err.message.split(':')[0], __err.sendid ? { sendid: __err.sendid } : null);
            } else {
                this._raisePlatform('error.execution', __err.sendid ? { sendid: __err.sendid } : null);
            }
        }
    }

    _exit_error() {
        if (this._traceListener) this._traceListener.onStateExit('error', [...this.activeStates], Date.now());
        this._exitState('error');
    }

    _getEnabledTransition_error(event) {
        if (this._matchEvent(event.name, 'C0DI')) {
            return {
                source: 'error',
                priority: 9,
                exitSet: new Set(['error']),
                execute: () => {
                    if (this.activeStates.has('error')) {
                        this._exit_error();
                    }
                    if (this._traceListener) this._traceListener.onTransitionExecute('error', ['not_initialized'], event.name, null, Date.now());
                    if (this._traceListener) this._traceListener.onActionExecute('assign', {location: 'last_error', expr: '0'}, Date.now());
                    this._validateAndAssign('last_error', this._evalExpr('0', true));
                    if (this._traceListener) this._traceListener.onActionExecute('log', {label: 'FW', expr: '\'Re-init after error\''}, Date.now());
                    this._log('FW', this._evalExpr('\'Re-init after error\''));
                    if (!this.activeStates.has('operational')) {
                        this._enter_operational();
                    }
                    this._enter_not_initialized();
                }
            };
        }
        return null;
    }

    /**
     * Get the event ID for an event name.
     * @param {string} eventName - The event name
     * @returns {number} The event ID or EVT_UNKNOWN (9999) if not found
     */
    getEventId(eventName) {
        return this.constructor.EVENT_MAP.get(eventName) ?? this.constructor.EVT_UNKNOWN;
    }

    /**
     * Get the event name for an event ID.
     * @param {number} eventId - The event ID
     * @returns {string|null} The event name or null if unknown
     */
    getEventName(eventId) {
        switch (eventId) {
            case 5: return 'C0AS';
            case 9: return 'C0DF';
            case 6: return 'C0DS';
            case 8: return 'C0TR';
            case 4: return 'C0TP';
            case 7: return 'C0LW';
            case 10: return 'move.done';
            case 3: return 'C0ZA';
            case 11: return 'wash.done';
            case 1: return 'C0DI';
            case 2: return 'C0JM';
            default: return null;
        }
    }

    /**
     * Send an event using its integer ID for optimized O(1) dispatch.
     * This bypasses string-to-ID lookup for maximum performance.
     * @param {number} eventId - The event ID (use EVT_* constants)
     * @param {*} [data=null] - Optional event data
     */
    sendById(eventId, data = null) {
        if (this._finished) return;
        const eventName = this.getEventName(eventId);
        if (eventName === null) {
            console.warn('Unknown event ID:', eventId);
            return;
        }
        this.send(eventName, data);
    }

    _matchEvent(eventName, pattern) {
        if (pattern === '*' || pattern === '.*') return true;
        if (pattern === eventName) return true;

        // Prefix matching: 'foo' matches 'foo.bar'
        if (eventName.startsWith(pattern + '.')) return true;

        // Handle space-separated patterns
        const patterns = pattern.split(/\s+/);
        for (const p of patterns) {
            if (p === '*' || p === '.*') return true;
            if (p === eventName) return true;
            if (p.endsWith('.*') && eventName.startsWith(p.slice(0, -2))) return true;
            if (eventName.startsWith(p + '.')) return true;
        }

        return false;
    }

    /**
     * Check if an event ID matches a pattern (internal use).
     * Uses integer comparison for known events, falls back to string for unknown.
     */
    _matchEventById(eventId, eventName, patternId, pattern) {
        // Fast path: wildcard '*' matches everything
        if (pattern === '*' || pattern === '.*') return true;

        // Fast path: exact match by ID (for known events)
        if (eventId !== this.constructor.EVT_UNKNOWN && eventId === patternId) return true;

        // For unknown events or prefix matching, fall back to string comparison
        return this._matchEvent(eventName, pattern);
    }


    static _ALL_EVENTS = new Set(['C0DI', 'C0JM', 'C0ZA', 'C0TP', 'C0AS', 'C0DS', 'C0LW', 'C0TR', 'C0DF', 'move.done', 'wash.done']);

    /**
     * Returns all events this state machine can react to.
     * @returns {Set<string>}
     */
    getAllEvents() {
        return new Set(this.constructor._ALL_EVENTS);
    }

    /**
     * Returns events currently enabled based on active states and guard conditions.
     * @returns {Set<string>}
     */
    getEnabledEvents() {
        const enabled = new Set();
        if (this.activeStates.has('not_initialized')) {
            enabled.add('C0DI');
        }
        if (this.activeStates.has('idle')) {
            enabled.add('C0JM');
            enabled.add('C0ZA');
        }
        if (this.activeStates.has('no_tip')) {
            enabled.add('C0TP');
            enabled.add('C0AS');
            enabled.add('C0DS');
        }
        if (this.activeStates.has('tip_fitted')) {
            enabled.add('C0LW');
        }
        if (this.activeStates.has('tip_empty')) {
            enabled.add('C0AS');
            enabled.add('C0TR');
        }
        if (this.activeStates.has('tip_loaded')) {
            try { if (this._evalCond('_event.data.dm == 4')) {
                enabled.add('C0DS');
            } } catch (_ge) { /* guard not evaluable — exclude */ }
            try { if (this._evalCond('_event.data.dm == 2 || _event.data.dm == 3')) {
                enabled.add('C0DS');
            } } catch (_ge) { /* guard not evaluable — exclude */ }
            enabled.add('C0DS');
            enabled.add('C0AS');
            enabled.add('C0DF');
            enabled.add('C0TR');
        }
        if (this.activeStates.has('moving')) {
            enabled.add('move.done');
        }
        if (this.activeStates.has('washing')) {
            enabled.add('wash.done');
        }
        if (this.activeStates.has('error')) {
            enabled.add('C0DI');
        }
        return enabled;
    }

    /**
     * Returns all events a specific state can react to (including ancestor transitions).
     * @param {string} stateId
     * @returns {Set<string>}
     */
    getEventsForState(stateId) {
        switch (stateId) {
            case 'not_initialized': return new Set(['C0DI']);
            case 'idle': return new Set(['C0JM', 'C0ZA']);
            case 'no_tip': return new Set(['C0TP', 'C0AS', 'C0DS', 'C0JM', 'C0ZA']);
            case 'tip_fitted': return new Set(['C0LW', 'C0JM', 'C0ZA']);
            case 'tip_empty': return new Set(['C0AS', 'C0TR', 'C0LW', 'C0JM', 'C0ZA']);
            case 'tip_loaded': return new Set(['C0DS', 'C0AS', 'C0DF', 'C0TR', 'C0LW', 'C0JM', 'C0ZA']);
            case 'moving': return new Set(['move.done']);
            case 'washing': return new Set(['wash.done']);
            case 'error': return new Set(['C0DI']);
            default: return new Set();
        }
    }

    /**
     * Returns events currently enabled for a specific active state (guard-aware).
     * @param {string} stateId
     * @returns {Set<string>}
     */
    getEnabledEventsForState(stateId) {
        if (!this.activeStates.has(stateId)) return new Set();
        const enabled = new Set();
        switch (stateId) {
            case 'not_initialized': {
                enabled.add('C0DI');
                break;
            }
            case 'idle': {
                enabled.add('C0JM');
                enabled.add('C0ZA');
                break;
            }
            case 'no_tip': {
                enabled.add('C0TP');
                enabled.add('C0AS');
                enabled.add('C0DS');
                enabled.add('C0JM');
                enabled.add('C0ZA');
                break;
            }
            case 'tip_fitted': {
                enabled.add('C0LW');
                enabled.add('C0JM');
                enabled.add('C0ZA');
                break;
            }
            case 'tip_empty': {
                enabled.add('C0AS');
                enabled.add('C0TR');
                enabled.add('C0LW');
                enabled.add('C0JM');
                enabled.add('C0ZA');
                break;
            }
            case 'tip_loaded': {
                try { if (this._evalCond('_event.data.dm == 4')) {
                    enabled.add('C0DS');
                } } catch (_ge) { /* guard not evaluable */ }
                try { if (this._evalCond('_event.data.dm == 2 || _event.data.dm == 3')) {
                    enabled.add('C0DS');
                } } catch (_ge) { /* guard not evaluable */ }
                enabled.add('C0DS');
                enabled.add('C0AS');
                enabled.add('C0DF');
                enabled.add('C0TR');
                enabled.add('C0LW');
                enabled.add('C0JM');
                enabled.add('C0ZA');
                break;
            }
            case 'moving': {
                enabled.add('move.done');
                break;
            }
            case 'washing': {
                enabled.add('wash.done');
                break;
            }
            case 'error': {
                enabled.add('C0DI');
                break;
            }
            default: break;
        }
        return enabled;
    }


    /**
     * Add a trace listener for debugging/monitoring.
     * @param {Object} listener - Listener object with trace callback methods:
     *   - onSessionStart(sessionId, machineName, timestamp)
     *   - onSessionEnd(sessionId, finalStates, timestamp)
     *   - onStateEnter(stateId, activeStates, timestamp)
     *   - onStateExit(stateId, activeStates, timestamp)
     *   - onTransitionExecute(sourceId, targetIds, event, cond, timestamp)
     *   - onEventReceived(eventName, eventType, timestamp)
     *   - onEventProcessed(eventName, timestamp)
     *   - onActionExecute(actionType, details, timestamp)
     */
    addTraceListener(listener) {
        this._traceListener = listener;
    }

}

// CommonJS export for Node.js compatibility
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { PipChannelSM };
}
