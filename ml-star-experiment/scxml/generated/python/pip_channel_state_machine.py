# Generated by SCXML-Gen Python Code Generator
# Do not edit manually

from __future__ import annotations
from typing import Any

from scxmlgen import TranspiledStateMachine, Event, DataModelType

class PipChannelStateMachine(TranspiledStateMachine):
    """Generated SCXML state machine."""

    # State Constants
    S_OPERATIONAL = 0
    S_NOT_INITIALIZED = 1
    S_IDLE = 2
    S_NO_TIP = 3
    S_TIP_FITTED = 4
    S_TIP_EMPTY = 5
    S_TIP_LOADED = 6
    S_MOVING = 7
    S_WASHING = 8
    S_ERROR = 9

    # Event Constants
    EVT_C0AS = 5
    EVT_C0DF = 9
    EVT_C0DS = 6
    EVT_C0TR = 8
    EVT_C0TP = 4
    EVT_C0LW = 7
    EVT_JM_COMPLETE = 10
    EVT_LW_COMPLETE = 11
    EVT_C0ZA = 3
    EVT_C0DI = 1
    EVT_C0JM = 2
    EVT_UNKNOWN = 99999

    _STATE_NAMES: list[str] = [
        "operational",
        "not_initialized",
        "idle",
        "no_tip",
        "tip_fitted",
        "tip_empty",
        "tip_loaded",
        "moving",
        "washing",
        "error"
    ]

    _EVENT_MAP: dict[str, int] = {
        "C0AS": 5,
        "C0DF": 9,
        "C0DS": 6,
        "C0TR": 8,
        "C0TP": 4,
        "C0LW": 7,
        "JM.complete": 10,
        "LW.complete": 11,
        "C0ZA": 3,
        "C0DI": 1,
        "C0JM": 2,
    }

    def __init__(self) -> None:
        super().__init__("HamiltonSTAR_PipChannel", DataModelType.ECMASCRIPT, 10)
        # Pre-allocated sets for dispatch (avoids allocation per event)
        self._dispatch_snapshot: set[int] = set()
        self._dispatch_exited: set[int] = set()

    def _configure_data_model(self) -> None:
        try:
            self._data_model.execute_script("channel_count = 16")
        except Exception:
            self._data_model.set_undefined("channel_count")
            self._runtime_context.raise_error_execution("Failed to initialize data element: channel_count")
        try:
            self._data_model.execute_script("tip_pattern = 0")
        except Exception:
            self._data_model.set_undefined("tip_pattern")
            self._runtime_context.raise_error_execution("Failed to initialize data element: tip_pattern")
        try:
            self._data_model.execute_script("tip_type = -1")
        except Exception:
            self._data_model.set_undefined("tip_type")
            self._runtime_context.raise_error_execution("Failed to initialize data element: tip_type")
        try:
            self._data_model.execute_script("volume_held_01ul = 0")
        except Exception:
            self._data_model.set_undefined("volume_held_01ul")
            self._runtime_context.raise_error_execution("Failed to initialize data element: volume_held_01ul")
        try:
            self._data_model.execute_script("position_x_01mm = 0")
        except Exception:
            self._data_model.set_undefined("position_x_01mm")
            self._runtime_context.raise_error_execution("Failed to initialize data element: position_x_01mm")
        try:
            self._data_model.execute_script("position_y_01mm = 0")
        except Exception:
            self._data_model.set_undefined("position_y_01mm")
            self._runtime_context.raise_error_execution("Failed to initialize data element: position_y_01mm")
        try:
            self._data_model.execute_script("position_z_01mm = 0")
        except Exception:
            self._data_model.set_undefined("position_z_01mm")
            self._runtime_context.raise_error_execution("Failed to initialize data element: position_z_01mm")
        try:
            self._data_model.execute_script("last_lld_height_01mm = 0")
        except Exception:
            self._data_model.set_undefined("last_lld_height_01mm")
            self._runtime_context.raise_error_execution("Failed to initialize data element: last_lld_height_01mm")
        try:
            self._data_model.execute_script("last_error = 0")
        except Exception:
            self._data_model.set_undefined("last_error")
            self._runtime_context.raise_error_execution("Failed to initialize data element: last_error")
        try:
            self._data_model.execute_script("tadm_status = 'ok'")
        except Exception:
            self._data_model.set_undefined("tadm_status")
            self._runtime_context.raise_error_execution("Failed to initialize data element: tadm_status")
        try:
            self._data_model.execute_script("liquid_class = 'default'")
        except Exception:
            self._data_model.set_undefined("liquid_class")
            self._runtime_context.raise_error_execution("Failed to initialize data element: liquid_class")

    def _enter_initial_state(self) -> None:
        self._enter_S_OPERATIONAL()

    def _get_event_id(self, event_name: str) -> int:
        match event_name:
            case "C0AS": return 5
            case "C0DF": return 9
            case "C0DS": return 6
            case "C0TR": return 8
            case "C0TP": return 4
            case "C0LW": return 7
            case "JM.complete": return 10
            case "LW.complete": return 11
            case "C0ZA": return 3
            case "C0DI": return 1
            case "C0JM": return 2
            case _: return self.EVT_UNKNOWN

    def _get_event_name_by_id(self, event_id: int) -> str | None:
        """Gets the event name for an event ID (for send_by_id)."""
        match event_id:
            case self.EVT_C0AS: return "C0AS"
            case self.EVT_C0DF: return "C0DF"
            case self.EVT_C0DS: return "C0DS"
            case self.EVT_C0TR: return "C0TR"
            case self.EVT_C0TP: return "C0TP"
            case self.EVT_C0LW: return "C0LW"
            case self.EVT_JM_COMPLETE: return "JM.complete"
            case self.EVT_LW_COMPLETE: return "LW.complete"
            case self.EVT_C0ZA: return "C0ZA"
            case self.EVT_C0DI: return "C0DI"
            case self.EVT_C0JM: return "C0JM"
            case _: return None

    def _get_state_index(self, state_id: str) -> int:
        match state_id:
            case "operational": return self.S_OPERATIONAL
            case "not_initialized": return self.S_NOT_INITIALIZED
            case "idle": return self.S_IDLE
            case "no_tip": return self.S_NO_TIP
            case "tip_fitted": return self.S_TIP_FITTED
            case "tip_empty": return self.S_TIP_EMPTY
            case "tip_loaded": return self.S_TIP_LOADED
            case "moving": return self.S_MOVING
            case "washing": return self.S_WASHING
            case "error": return self.S_ERROR
            case _: return -1

    def _get_state_name(self, index: int) -> str | None:
        return self._STATE_NAMES[index] if 0 <= index < len(self._STATE_NAMES) else None

    def _get_current_event_name(self) -> str | None:
        return self._runtime_context.current_event.name if self._runtime_context.current_event else None

    _ALL_EVENTS = frozenset(["C0DI", "C0JM", "C0ZA", "C0TP", "C0AS", "C0DS", "C0LW", "C0TR", "C0DF", "JM.complete", "LW.complete"])

    def get_all_events(self) -> frozenset:
        """Returns all events this state machine can react to."""
        return self._ALL_EVENTS

    def get_enabled_events(self) -> frozenset:
        """Returns events currently enabled based on active states and guard conditions."""
        enabled = set()
        if self.S_NOT_INITIALIZED in self._active_states:
            enabled.add("C0DI")
        if self.S_IDLE in self._active_states:
            enabled.add("C0JM")
            enabled.add("C0ZA")
        if self.S_NO_TIP in self._active_states:
            enabled.add("C0TP")
            enabled.add("C0AS")
            enabled.add("C0DS")
        if self.S_TIP_FITTED in self._active_states:
            enabled.add("C0LW")
        if self.S_TIP_EMPTY in self._active_states:
            enabled.add("C0AS")
            enabled.add("C0TR")
        if self.S_TIP_LOADED in self._active_states:
            try:
                _ge_ok, _ge_val = self._data_model.try_evaluate_boolean("_event.data.dm <= 1")
            except Exception:
                _ge_ok, _ge_val = False, False
            if _ge_ok and _ge_val:
                enabled.add("C0DS")
            try:
                _ge_ok, _ge_val = self._data_model.try_evaluate_boolean("_event.data.dm == 2 || _event.data.dm == 3")
            except Exception:
                _ge_ok, _ge_val = False, False
            if _ge_ok and _ge_val:
                enabled.add("C0DS")
            try:
                _ge_ok, _ge_val = self._data_model.try_evaluate_boolean("_event.data.dm == 4")
            except Exception:
                _ge_ok, _ge_val = False, False
            if _ge_ok and _ge_val:
                enabled.add("C0DS")
            enabled.add("C0TR")
            enabled.add("C0DF")
        if self.S_MOVING in self._active_states:
            enabled.add("JM.complete")
        if self.S_WASHING in self._active_states:
            enabled.add("LW.complete")
        if self.S_ERROR in self._active_states:
            enabled.add("C0DI")
        return frozenset(enabled)

    def get_events_for_state(self, state_id: str) -> frozenset:
        """Returns all events a specific state can react to (including ancestor transitions)."""
        idx = self._get_state_index(state_id)
        if idx < 0: return frozenset()
        match idx:
            case self.S_NOT_INITIALIZED: return frozenset(["C0DI"])
            case self.S_IDLE: return frozenset(["C0JM", "C0ZA"])
            case self.S_NO_TIP: return frozenset(["C0TP", "C0AS", "C0DS", "C0JM", "C0ZA"])
            case self.S_TIP_FITTED: return frozenset(["C0LW", "C0JM", "C0ZA"])
            case self.S_TIP_EMPTY: return frozenset(["C0AS", "C0TR", "C0LW", "C0JM", "C0ZA"])
            case self.S_TIP_LOADED: return frozenset(["C0DS", "C0TR", "C0DF", "C0LW", "C0JM", "C0ZA"])
            case self.S_MOVING: return frozenset(["JM.complete"])
            case self.S_WASHING: return frozenset(["LW.complete"])
            case self.S_ERROR: return frozenset(["C0DI"])
            case _: return frozenset()

    def get_enabled_events_for_state(self, state_id: str) -> frozenset:
        """Returns events currently enabled for a specific active state (guard-aware)."""
        idx = self._get_state_index(state_id)
        if idx < 0 or idx not in self._active_states: return frozenset()
        enabled = set()
        match idx:
            case self.S_NOT_INITIALIZED:
                enabled.add("C0DI")
            case self.S_IDLE:
                enabled.add("C0JM")
                enabled.add("C0ZA")
            case self.S_NO_TIP:
                enabled.add("C0TP")
                enabled.add("C0AS")
                enabled.add("C0DS")
                enabled.add("C0JM")
                enabled.add("C0ZA")
            case self.S_TIP_FITTED:
                enabled.add("C0LW")
                enabled.add("C0JM")
                enabled.add("C0ZA")
            case self.S_TIP_EMPTY:
                enabled.add("C0AS")
                enabled.add("C0TR")
                enabled.add("C0LW")
                enabled.add("C0JM")
                enabled.add("C0ZA")
            case self.S_TIP_LOADED:
                try:
                    _ge_ok, _ge_val = self._data_model.try_evaluate_boolean("_event.data.dm <= 1")
                except Exception:
                    _ge_ok, _ge_val = False, False
                if _ge_ok and _ge_val:
                    enabled.add("C0DS")
                try:
                    _ge_ok, _ge_val = self._data_model.try_evaluate_boolean("_event.data.dm == 2 || _event.data.dm == 3")
                except Exception:
                    _ge_ok, _ge_val = False, False
                if _ge_ok and _ge_val:
                    enabled.add("C0DS")
                try:
                    _ge_ok, _ge_val = self._data_model.try_evaluate_boolean("_event.data.dm == 4")
                except Exception:
                    _ge_ok, _ge_val = False, False
                if _ge_ok and _ge_val:
                    enabled.add("C0DS")
                enabled.add("C0TR")
                enabled.add("C0DF")
                enabled.add("C0LW")
                enabled.add("C0JM")
                enabled.add("C0ZA")
            case self.S_MOVING:
                enabled.add("JM.complete")
            case self.S_WASHING:
                enabled.add("LW.complete")
            case self.S_ERROR:
                enabled.add("C0DI")
        return frozenset(enabled)

    def _dispatch_event(self, event_id: int, event: Event) -> bool:
        handled = False
        # Pre-allocated sets for dispatch (performance optimization)
        snapshot = self._dispatch_snapshot
        snapshot.clear()
        snapshot.update(self._active_states)
        exited = self._dispatch_exited
        exited.clear()
        self._transient_fired_states.clear()

        for i in snapshot:
            if i in exited:
                continue
            exited.add(i)

            match i:
                case self.S_NOT_INITIALIZED:
                    if self._state_S_NOT_INITIALIZED(event_id, event):
                        handled = True
                case self.S_NO_TIP:
                    if self._state_S_NO_TIP(event_id, event):
                        handled = True
                case self.S_TIP_EMPTY:
                    if self._state_S_TIP_EMPTY(event_id, event):
                        handled = True
                case self.S_TIP_LOADED:
                    if self._state_S_TIP_LOADED(event_id, event):
                        handled = True
                case self.S_MOVING:
                    if self._state_S_MOVING(event_id, event):
                        handled = True
                case self.S_WASHING:
                    if self._state_S_WASHING(event_id, event):
                        handled = True
                case self.S_ERROR:
                    if self._state_S_ERROR(event_id, event):
                        handled = True
                case _: pass

        return handled

    def _check_eventless_transitions(self) -> None:
        while self._do_eventless_step():
            if self._finished:
                break

    def _do_eventless_step(self) -> bool:
        return False

    def _enter_S_OPERATIONAL(self) -> None:
        self._logger("Enter operational")
        self._active_states.add(self.S_OPERATIONAL)
        if self._trace_listener: self._trace_listener.on_state_enter("operational", __import__('time').time_ns() // 1000)
        self._enter_S_NOT_INITIALIZED()

    def _enter_S_OPERATIONAL_no_children(self) -> None:
        self._logger("Enter operational (no children)")
        self._active_states.add(self.S_OPERATIONAL)
        if self._trace_listener: self._trace_listener.on_state_enter("operational", __import__('time').time_ns() // 1000)

    def _exit_S_OPERATIONAL(self) -> None:
        if self.S_OPERATIONAL not in self._active_states: return
        self._exit_S_ERROR()
        self._exit_S_WASHING()
        self._exit_S_MOVING()
        self._exit_S_IDLE()
        self._exit_S_NOT_INITIALIZED()
        self._logger("Exit operational")
        if self._trace_listener: self._trace_listener.on_state_exit("operational", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_OPERATIONAL)

    def _state_S_OPERATIONAL(self, event_id: int, event: Event) -> bool:
        return False

    def _enter_S_NOT_INITIALIZED(self) -> None:
        self._logger("Enter not_initialized")
        self._active_states.add(self.S_NOT_INITIALIZED)
        if self._trace_listener: self._trace_listener.on_state_enter("not_initialized", __import__('time').time_ns() // 1000)
        try:
            try:
                self._data_model.assign("tip_pattern", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_pattern")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_pattern = 0"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_type", "-1")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_type")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_type = -1"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("volume_held_01ul", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = 0"}, __import__('time').time_ns() // 1000)
        except Exception:
            pass  # Error already raised by action

    def _exit_S_NOT_INITIALIZED(self) -> None:
        if self.S_NOT_INITIALIZED not in self._active_states: return
        self._logger("Exit not_initialized")
        if self._trace_listener: self._trace_listener.on_state_exit("not_initialized", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_NOT_INITIALIZED)

    def _state_S_NOT_INITIALIZED(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_C0DI or self._matches_event_descriptor(self._get_current_event_name(), "C0DI")):
            if self.S_NOT_INITIALIZED in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_NOT_INITIALIZED()
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'Initialize PIP channels'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Initialize PIP channels'"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_pattern", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_pattern")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_pattern = 0"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_type", "-1")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_type")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_type = -1"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("volume_held_01ul", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = 0"}, __import__('time').time_ns() // 1000)
            self._raise_internal("DI.complete")
            if self._trace_listener: self._trace_listener.on_action_execute("raise", {"details": "DI.complete"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("not_initialized", "no_tip", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_IDLE()
            self._enter_S_NO_TIP()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        return self._state_S_OPERATIONAL(event_id, event)

    def _enter_S_IDLE(self) -> None:
        self._logger("Enter idle")
        self._active_states.add(self.S_IDLE)
        if self._trace_listener: self._trace_listener.on_state_enter("idle", __import__('time').time_ns() // 1000)
        self._enter_S_NO_TIP()

    def _enter_S_IDLE_no_children(self) -> None:
        self._logger("Enter idle (no children)")
        self._active_states.add(self.S_IDLE)
        if self._trace_listener: self._trace_listener.on_state_enter("idle", __import__('time').time_ns() // 1000)

    def _exit_S_IDLE(self) -> None:
        if self.S_IDLE not in self._active_states: return
        self._exit_S_TIP_FITTED()
        self._exit_S_NO_TIP()
        self._logger("Exit idle")
        if self._trace_listener: self._trace_listener.on_state_exit("idle", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_IDLE)

    def _state_S_IDLE(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_C0JM or self._matches_event_descriptor(self._get_current_event_name(), "C0JM")):
            if self.S_IDLE in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_IDLE()
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'Move to position'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Move to position'"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("position_x_01mm", "_event.data.xp")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: position_x_01mm")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "position_x_01mm = _event.data.xp"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("position_y_01mm", "_event.data.yp")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: position_y_01mm")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "position_y_01mm = _event.data.yp"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("position_z_01mm", "_event.data.zp")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: position_z_01mm")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "position_z_01mm = _event.data.zp"}, __import__('time').time_ns() // 1000)
            self._raise_internal("JM.complete")
            if self._trace_listener: self._trace_listener.on_action_execute("raise", {"details": "JM.complete"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("idle", "moving", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_MOVING()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        if (event_id == self.EVT_C0ZA or self._matches_event_descriptor(self._get_current_event_name(), "C0ZA")):
            if self.S_IDLE in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_IDLE()
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'Move all PIP to Z-safety'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Move all PIP to Z-safety'"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("position_z_01mm", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: position_z_01mm")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "position_z_01mm = 0"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("idle", "idle", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_IDLE()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        return self._state_S_OPERATIONAL(event_id, event)

    def _enter_S_NO_TIP(self) -> None:
        self._logger("Enter no_tip")
        self._active_states.add(self.S_NO_TIP)
        if self._trace_listener: self._trace_listener.on_state_enter("no_tip", __import__('time').time_ns() // 1000)

    def _exit_S_NO_TIP(self) -> None:
        if self.S_NO_TIP not in self._active_states: return
        self._logger("Exit no_tip")
        if self._trace_listener: self._trace_listener.on_state_exit("no_tip", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_NO_TIP)

    def _state_S_NO_TIP(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_C0TP or self._matches_event_descriptor(self._get_current_event_name(), "C0TP")):
            if self.S_NO_TIP in self._transient_fired_states: return False
            if self.S_IDLE in self._transient_fired_states: return False
            self._exit_S_NO_TIP()
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'Tip pick-up'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Tip pick-up'"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_pattern", "_event.data.tm")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_pattern")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_pattern = _event.data.tm"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_type", "_event.data.tt")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_type")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_type = _event.data.tt"}, __import__('time').time_ns() // 1000)
            self._raise_internal("TP.complete")
            if self._trace_listener: self._trace_listener.on_action_execute("raise", {"details": "TP.complete"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("no_tip", "tip_empty", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_TIP_FITTED()
            self._enter_S_TIP_EMPTY()
            self._transient_fired_states.add(self.S_IDLE)
            return True
        if (event_id == self.EVT_C0AS or self._matches_event_descriptor(self._get_current_event_name(), "C0AS")):
            if self.S_NO_TIP in self._transient_fired_states: return False
            if self.S_IDLE in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_IDLE()
            try:
                self._data_model.assign("last_error", "8")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: last_error")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "last_error = 8"}, __import__('time').time_ns() // 1000)
            self._logger("FW_ERROR: " + str(self._data_model.evaluate_to_string("'Cannot aspirate: no tip (error 08)'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW_ERROR: 'Cannot aspirate: no tip (error 08)'"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("no_tip", "error", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_ERROR()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        if (event_id == self.EVT_C0DS or self._matches_event_descriptor(self._get_current_event_name(), "C0DS")):
            if self.S_NO_TIP in self._transient_fired_states: return False
            if self.S_IDLE in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_IDLE()
            try:
                self._data_model.assign("last_error", "8")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: last_error")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "last_error = 8"}, __import__('time').time_ns() // 1000)
            self._logger("FW_ERROR: " + str(self._data_model.evaluate_to_string("'Cannot dispense: no tip (error 08)'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW_ERROR: 'Cannot dispense: no tip (error 08)'"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("no_tip", "error", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_ERROR()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        return self._state_S_IDLE(event_id, event)

    def _enter_S_TIP_FITTED(self) -> None:
        self._logger("Enter tip_fitted")
        self._active_states.add(self.S_TIP_FITTED)
        if self._trace_listener: self._trace_listener.on_state_enter("tip_fitted", __import__('time').time_ns() // 1000)
        self._enter_S_TIP_EMPTY()

    def _enter_S_TIP_FITTED_no_children(self) -> None:
        self._logger("Enter tip_fitted (no children)")
        self._active_states.add(self.S_TIP_FITTED)
        if self._trace_listener: self._trace_listener.on_state_enter("tip_fitted", __import__('time').time_ns() // 1000)

    def _exit_S_TIP_FITTED(self) -> None:
        if self.S_TIP_FITTED not in self._active_states: return
        self._exit_S_TIP_LOADED()
        self._exit_S_TIP_EMPTY()
        self._logger("Exit tip_fitted")
        if self._trace_listener: self._trace_listener.on_state_exit("tip_fitted", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_TIP_FITTED)

    def _state_S_TIP_FITTED(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_C0LW or self._matches_event_descriptor(self._get_current_event_name(), "C0LW")):
            if self.S_TIP_FITTED in self._transient_fired_states: return False
            if self.S_IDLE in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_IDLE()
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'DC Wash'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'DC Wash'"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("volume_held_01ul", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = 0"}, __import__('time').time_ns() // 1000)
            self._raise_internal("LW.complete")
            if self._trace_listener: self._trace_listener.on_action_execute("raise", {"details": "LW.complete"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("tip_fitted", "washing", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_WASHING()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        return self._state_S_IDLE(event_id, event)

    def _enter_S_TIP_EMPTY(self) -> None:
        self._logger("Enter tip_empty")
        self._active_states.add(self.S_TIP_EMPTY)
        if self._trace_listener: self._trace_listener.on_state_enter("tip_empty", __import__('time').time_ns() // 1000)
        try:
            try:
                self._data_model.assign("volume_held_01ul", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = 0"}, __import__('time').time_ns() // 1000)
        except Exception:
            pass  # Error already raised by action

    def _exit_S_TIP_EMPTY(self) -> None:
        if self.S_TIP_EMPTY not in self._active_states: return
        self._logger("Exit tip_empty")
        if self._trace_listener: self._trace_listener.on_state_exit("tip_empty", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_TIP_EMPTY)

    def _state_S_TIP_EMPTY(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_C0AS or self._matches_event_descriptor(self._get_current_event_name(), "C0AS")):
            if self.S_TIP_EMPTY in self._transient_fired_states: return False
            if self.S_TIP_FITTED in self._transient_fired_states: return False
            self._exit_S_TIP_EMPTY()
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'Aspirate: vol=' + _event.data.av")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Aspirate: vol=' + _event.data.av"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("volume_held_01ul", "_event.data.av")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = _event.data.av"}, __import__('time').time_ns() // 1000)
            self._raise_internal("AS.complete")
            if self._trace_listener: self._trace_listener.on_action_execute("raise", {"details": "AS.complete"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("tip_empty", "tip_loaded", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_TIP_LOADED()
            self._transient_fired_states.add(self.S_TIP_FITTED)
            return True
        if (event_id == self.EVT_C0TR or self._matches_event_descriptor(self._get_current_event_name(), "C0TR")):
            if self.S_TIP_EMPTY in self._transient_fired_states: return False
            if self.S_TIP_FITTED in self._transient_fired_states: return False
            if self.S_IDLE in self._transient_fired_states: return False
            self._exit_S_TIP_FITTED()
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'Tip eject'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Tip eject'"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_pattern", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_pattern")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_pattern = 0"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_type", "-1")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_type")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_type = -1"}, __import__('time').time_ns() // 1000)
            self._raise_internal("TR.complete")
            if self._trace_listener: self._trace_listener.on_action_execute("raise", {"details": "TR.complete"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("tip_empty", "no_tip", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_NO_TIP()
            self._transient_fired_states.add(self.S_IDLE)
            return True
        return self._state_S_TIP_FITTED(event_id, event)

    def _enter_S_TIP_LOADED(self) -> None:
        self._logger("Enter tip_loaded")
        self._active_states.add(self.S_TIP_LOADED)
        if self._trace_listener: self._trace_listener.on_state_enter("tip_loaded", __import__('time').time_ns() // 1000)

    def _exit_S_TIP_LOADED(self) -> None:
        if self.S_TIP_LOADED not in self._active_states: return
        self._logger("Exit tip_loaded")
        if self._trace_listener: self._trace_listener.on_state_exit("tip_loaded", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_TIP_LOADED)

    def _state_S_TIP_LOADED(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_C0DS or self._matches_event_descriptor(self._get_current_event_name(), "C0DS")):
            success, cond_result = self._data_model.try_evaluate_boolean("_event.data.dm <= 1")
            if not success:
                self._runtime_context.raise_error_execution("Condition evaluation failed: _event.data.dm <= 1")
            if cond_result:
                if self.S_TIP_LOADED in self._transient_fired_states: return False
                if self.S_TIP_FITTED in self._transient_fired_states: return False
                self._exit_S_TIP_LOADED()
                self._logger("FW: " + str(self._data_model.evaluate_to_string("'Dispense (jet): vol=' + _event.data.dv")))
                if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Dispense (jet): vol=' + _event.data.dv"}, __import__('time').time_ns() // 1000)
                try:
                    self._data_model.assign("volume_held_01ul", "Math.max(0, volume_held_01ul - _event.data.dv)")
                except Exception:
                    self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                    raise  # Re-raise so surrounding foreach can break
                if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = Math.max(0, volume_held_01ul - _event.data.dv)"}, __import__('time').time_ns() // 1000)
                self._raise_internal("DS.complete")
                if self._trace_listener: self._trace_listener.on_action_execute("raise", {"details": "DS.complete"}, __import__('time').time_ns() // 1000)
                if self._trace_listener: self._trace_listener.on_transition("tip_loaded", "tip_empty", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
                self._enter_S_TIP_EMPTY()
                self._transient_fired_states.add(self.S_TIP_FITTED)
                return True
        if (event_id == self.EVT_C0DS or self._matches_event_descriptor(self._get_current_event_name(), "C0DS")):
            success, cond_result = self._data_model.try_evaluate_boolean("_event.data.dm == 2 || _event.data.dm == 3")
            if not success:
                self._runtime_context.raise_error_execution("Condition evaluation failed: _event.data.dm == 2 || _event.data.dm == 3")
            if cond_result:
                if self.S_TIP_LOADED in self._transient_fired_states: return False
                if self.S_TIP_FITTED in self._transient_fired_states: return False
                self._exit_S_TIP_LOADED()
                self._logger("FW: " + str(self._data_model.evaluate_to_string("'Dispense (partial): vol=' + _event.data.dv")))
                if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Dispense (partial): vol=' + _event.data.dv"}, __import__('time').time_ns() // 1000)
                try:
                    self._data_model.assign("volume_held_01ul", "Math.max(0, volume_held_01ul - _event.data.dv)")
                except Exception:
                    self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                    raise  # Re-raise so surrounding foreach can break
                if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = Math.max(0, volume_held_01ul - _event.data.dv)"}, __import__('time').time_ns() // 1000)
                if self._trace_listener: self._trace_listener.on_transition("tip_loaded", "tip_loaded", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
                self._enter_S_TIP_LOADED()
                self._transient_fired_states.add(self.S_TIP_FITTED)
                return True
        if (event_id == self.EVT_C0DS or self._matches_event_descriptor(self._get_current_event_name(), "C0DS")):
            success, cond_result = self._data_model.try_evaluate_boolean("_event.data.dm == 4")
            if not success:
                self._runtime_context.raise_error_execution("Condition evaluation failed: _event.data.dm == 4")
            if cond_result:
                if self.S_TIP_LOADED in self._transient_fired_states: return False
                if self.S_TIP_FITTED in self._transient_fired_states: return False
                self._exit_S_TIP_LOADED()
                self._logger("FW: " + str(self._data_model.evaluate_to_string("'Dispense (empty tip)'")))
                if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Dispense (empty tip)'"}, __import__('time').time_ns() // 1000)
                try:
                    self._data_model.assign("volume_held_01ul", "0")
                except Exception:
                    self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                    raise  # Re-raise so surrounding foreach can break
                if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = 0"}, __import__('time').time_ns() // 1000)
                if self._trace_listener: self._trace_listener.on_transition("tip_loaded", "tip_empty", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
                self._enter_S_TIP_EMPTY()
                self._transient_fired_states.add(self.S_TIP_FITTED)
                return True
        if (event_id == self.EVT_C0TR or self._matches_event_descriptor(self._get_current_event_name(), "C0TR")):
            if self.S_TIP_LOADED in self._transient_fired_states: return False
            if self.S_TIP_FITTED in self._transient_fired_states: return False
            if self.S_IDLE in self._transient_fired_states: return False
            self._exit_S_TIP_FITTED()
            self._logger("FW_WARN: " + str(self._data_model.evaluate_to_string("'Tip eject with liquid'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW_WARN: 'Tip eject with liquid'"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("volume_held_01ul", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = 0"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_pattern", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_pattern")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_pattern = 0"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("tip_type", "-1")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: tip_type")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "tip_type = -1"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("tip_loaded", "no_tip", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_NO_TIP()
            self._transient_fired_states.add(self.S_IDLE)
            return True
        if (event_id == self.EVT_C0DF or self._matches_event_descriptor(self._get_current_event_name(), "C0DF")):
            if self.S_TIP_LOADED in self._transient_fired_states: return False
            if self.S_TIP_FITTED in self._transient_fired_states: return False
            self._exit_S_TIP_LOADED()
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'Dispense on fly'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Dispense on fly'"}, __import__('time').time_ns() // 1000)
            try:
                self._data_model.assign("volume_held_01ul", "Math.max(0, volume_held_01ul - (_event.data.dv * _event.data.xi))")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: volume_held_01ul")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "volume_held_01ul = Math.max(0, volume_held_01ul - (_event.data.dv * _event.data.xi))"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("tip_loaded", "tip_loaded", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_TIP_LOADED()
            self._transient_fired_states.add(self.S_TIP_FITTED)
            return True
        return self._state_S_TIP_FITTED(event_id, event)

    def _enter_S_MOVING(self) -> None:
        self._logger("Enter moving")
        self._active_states.add(self.S_MOVING)
        if self._trace_listener: self._trace_listener.on_state_enter("moving", __import__('time').time_ns() // 1000)

    def _exit_S_MOVING(self) -> None:
        if self.S_MOVING not in self._active_states: return
        self._logger("Exit moving")
        if self._trace_listener: self._trace_listener.on_state_exit("moving", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_MOVING)

    def _state_S_MOVING(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_JM_COMPLETE or self._matches_event_descriptor(self._get_current_event_name(), "JM.complete")):
            if self.S_MOVING in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_MOVING()
            if self._trace_listener: self._trace_listener.on_transition("moving", "idle", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_IDLE()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        return self._state_S_OPERATIONAL(event_id, event)

    def _enter_S_WASHING(self) -> None:
        self._logger("Enter washing")
        self._active_states.add(self.S_WASHING)
        if self._trace_listener: self._trace_listener.on_state_enter("washing", __import__('time').time_ns() // 1000)

    def _exit_S_WASHING(self) -> None:
        if self.S_WASHING not in self._active_states: return
        self._logger("Exit washing")
        if self._trace_listener: self._trace_listener.on_state_exit("washing", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_WASHING)

    def _state_S_WASHING(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_LW_COMPLETE or self._matches_event_descriptor(self._get_current_event_name(), "LW.complete")):
            if self.S_WASHING in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_WASHING()
            if self._trace_listener: self._trace_listener.on_transition("washing", "tip_empty", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_IDLE()
            self._enter_S_TIP_FITTED()
            self._enter_S_TIP_EMPTY()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        return self._state_S_OPERATIONAL(event_id, event)

    def _enter_S_ERROR(self) -> None:
        self._logger("Enter error")
        self._active_states.add(self.S_ERROR)
        if self._trace_listener: self._trace_listener.on_state_enter("error", __import__('time').time_ns() // 1000)
        try:
            self._logger("PIP_ERROR: " + str(self._data_model.evaluate_to_string("'Error code: ' + last_error")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "PIP_ERROR: 'Error code: ' + last_error"}, __import__('time').time_ns() // 1000)
        except Exception:
            pass  # Error already raised by action

    def _exit_S_ERROR(self) -> None:
        if self.S_ERROR not in self._active_states: return
        self._logger("Exit error")
        if self._trace_listener: self._trace_listener.on_state_exit("error", __import__('time').time_ns() // 1000)
        self._active_states.discard(self.S_ERROR)

    def _state_S_ERROR(self, event_id: int, event: Event) -> bool:
        if (event_id == self.EVT_C0DI or self._matches_event_descriptor(self._get_current_event_name(), "C0DI")):
            if self.S_ERROR in self._transient_fired_states: return False
            if self.S_OPERATIONAL in self._transient_fired_states: return False
            self._exit_S_ERROR()
            try:
                self._data_model.assign("last_error", "0")
            except Exception:
                self._runtime_context.raise_error_execution("Assign failed: last_error")
                raise  # Re-raise so surrounding foreach can break
            if self._trace_listener: self._trace_listener.on_action_execute("assign", {"details": "last_error = 0"}, __import__('time').time_ns() // 1000)
            self._logger("FW: " + str(self._data_model.evaluate_to_string("'Re-init after error'")))
            if self._trace_listener: self._trace_listener.on_action_execute("log", {"details": "FW: 'Re-init after error'"}, __import__('time').time_ns() // 1000)
            if self._trace_listener: self._trace_listener.on_transition("error", "not_initialized", self._runtime_context.current_event.name if self._runtime_context.current_event else None, __import__('time').time_ns() // 1000)
            self._enter_S_NOT_INITIALIZED()
            self._transient_fired_states.add(self.S_OPERATIONAL)
            return True
        return self._state_S_OPERATIONAL(event_id, event)

    _HAS_EVENTLESS_TRANSITIONS = False

